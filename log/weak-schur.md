# Weak Schur WS(k) — двигать нижнюю границу WS(5) (и/или WS(6))

**Задача.** WS(k) = наибольшее N, при котором {1..N} разбивается на k частей, каждая weakly sum-free (нет x<y в части с x+y в той же части; 2x=z разрешено — это «weak»).

**Источник.** Скаут 16-06: INTEGERS 2017 (math.colgate.edu/~integers/r39/r39.pdf), Ageron et al. 2024 (arxiv 2112.03175).

> ## ⚠️ ЧИТАТЬ ПЕРВЫМ: в этом файле ДВЕ РАЗНЫЕ ВЕЛИЧИНЫ
>
> **WS(k)** — задача из литературы, определение выше.
>
> **M_chain(k)** — то же, но с ДОПОЛНИТЕЛЬНЫМ ограничением «монохромность по
> цепям»: f(2v) = f(v) для всех v. Это **моё** ограничение, не конвенция
> сообщества (проверено 06-09 04:00 чтением Bouzy 2015 до атомов: слово chain
> у них не встречается ни разу, редукция у них — про интервалы подряд идущих
> чисел). M_chain(k) ≤ WS(k), и **зазор растёт с k**:
>
> | k | 1 | 2 | 3 | 4 | 5 |
> |---|---|---|---|---|---|
> | WS(k) | 2 | 8 | 23 | 66 | ≥196 |
> | M_chain(k) | 2 | 8 | 22 | 45 | 89 |
> | зазор | 0 | 0 | 1 | 21 | ≥107 |
>
> **Почти всё в журнале ниже, начиная с 14-07 (N=90, c=5, f(45), cover-аргумент,
> MUS-ядра, rigid-корни) — утверждения о M_chain, а НЕ о WS.** В записях того
> периода это стоит оговоркой или не стоит вовсе: тогда я считала chain-mono
> WLOG-приёмом, и ошибка была не во лжи, а в ОБЛАСТИ. Записи намеренно не
> переписаны — журнал не подделывают; область несёт эта шапка.
>
> Как отличить при чтении: любое утверждение, где фигурируют цепи, удвоения,
> chain-rep, chain-roots, «45», «N=90» при k=5 — про M_chain. Утверждения про
> seam, |D|, max|D|, Eliahou-196, границы литературы — про WS.

**Статус известного (ТРЕБУЕТ верификации перед атакой):**
- WS(1)=2, WS(2)=8, WS(3)=23, WS(4)=66 — точные, подтверждены моим перебором (1-3) / литература (4).
- WS(5): lb=196 (Walker 1952), точное значение неизвестно. ⚠ **Статус после 2022 неясен — мог быть побит. Прибить точный текущий рекорд ДО атаки.**
- WS(6): lb=646 (Ageron 2024, эволюционными методами — значит метод работает, но фронтир активен).

**Цель (измеримо):** явное k-разбиение {1..N+1}, где N — истинный текущий lb. Для WS(5): {1..197}+ на 5 weakly-sum-free частей = рекорд.

**Верификатор:** `projects/frontier/verifiers/weak_schur.py`. ✅ Валидирован перебором против WS(1..3) + негативный контроль. Гейт: `verify_weak_schur(partition, N)`.

## Карта тупиков (не ходить сюда снова)
- (пусто — атака не начата)

## Инструменты (накоплены)
- `weak_schur.py::verify_weak_schur` — гейт (партиция → ok/reason).
- `weak_schur.py::find_partition(k,N)` — backtracking SAT, exhaustive (None = доказано невозможно). Воспроизводит WS(1..3) мгновенно.
- `weak_schur.py::ws_value(k)` — точное WS(k) перебором для малых k.

## Журнал (append, штамп вейка)
- **[16-06, с Саней]** Выбрала WS как первую задачу frontier (vs VdW/Golomb — податливее мутации, фронтир спит с 1952, ML не тронут). Написала верификатор САМА (гейт не делегирую). Валидация: ws_value воспроизвёл WS(1)=2/WS(2)=8/WS(3)=23 перебором за <0.1с, конструкции прошли гейт, `[[1,2,3]]` отвергнута. Основание заложено.
  - **Следующий шаг (шаг 0 перед атакой):** прибить ИСТИННЫЙ текущий рекорд WS(5) и WS(6) по литературе (Ageron 2024 + свежее) — не гнаться за фантомным 196. Параллельно: проверить, тянет ли `find_partition` до WS(4)=66 (UNSAT на 67) — это потолок наивного backtracking; если медленно → нужен умный поиск (hill-climbing/GA/SAT-редукция) для масштаба WS(5)~200.
  - **Затем (атака):** agent-team с параллельными стратегиями поиска + adversary, который гоняет верификатор на любом заявленном улучшении ДО того, как я его приму.
- **[16-06, позже]** Скаут добил истинный рекорд: **WS(5)≥196, верхняя граница НЕ доказана** — Walker 1952 заявил 196 без доказательства; явная конструкция построена SAT'ом лишь в 2012 (Eliahou et al., Comp.&Math.w.Appl. 63(1)). Никто не доказал невозможность 197. ⇒ {1..197} на 5 weakly-sum-free частей = **легитимная открытая задача, первое улучшение за 70+ лет**. WS(6)≥646 (Ageron 2021/22), явная конструкция WS(6)≥642 скачиваема (arxiv 2011.11292v5 ancillary `Wk_Schur_642_6-subsets.txt`). Ladder: WS(7)≥2146, WS(8)≥6976, WS(9)≥22536, WS(10)≥71256.
  - **Реалистично:** фронтир — обрыв, не склон. Малые WS(1..4) решены, WS(5)=196 сразу крутая. Эдж: современный Cadical >> солверов 2012 + continuity + комбинация методов.
  - **Наивный backtracking — потолок измерен:** find_partition(4,N): N=50 за 0.02с, **N=58 за 2114с (35 мин)**, дальше выход за бюджет. Лобовой перебор до WS(5)~196 не доживёт. → атаке нужен SAT. (data point → cognitive-limits CL-03).
  - **SAT-пайплайн построен и валидирован:** `search/sat_weak_schur.py` (CNF + Cadical via pysat + symmetry break, каждый SAT → через гейт). Воспроизвёл **WS(4)=66 за 14.6с, GATE PASS**, части [14,17,12,23]. UNSAT@67 считается (валидация exact). venv: `repos/proving-ground/.venv`, pysat.
  - **Сейчас (фон):** репродукция WS(5)=196 — валидирует пайплайн на реальной цели + даёт конструкцию для структурного изучения перед штурмом 197.
- **[16-06, поворот к creativity — Саня: compute скуден, brute-force не путь]** Признала: min-conflicts SA — мягкий молоток, не угол. Пивот на компьют-лёгкое.
  - **SAT-энкодер плохо масштабируется:** 196-репродукция и UNSAT@67 мололи без результата → сняты. SAT-линия на моём CPU нежизнеспособна для масштаба ~200.
  - **Структурный SA (`structured_search.py`):** дошёл до **conf=1 при runs=38** на N=196 (5 сидов). Гипотеза «records интервально-структурны» ПОДТВЕРЖДЕНА (низкое число прогонов). Застрял на последнем конфликте (tunnel).
  - **Min-conflicts (`minconf.py`):** tabu+kick, берёт conf=0 на N=66. Запущены контрольные 196/197 (как baseline молотка, НЕ основная ставка).
  - **Структурный анализ (`analyze_structure.py`) — НАХОДКА:** рекорды самоподобны — конструкция k содержит (k−1) точным префиксом; новый цвет берёт большой блок; рост ×2.87→2.97 (рекурсия Шура). Разреженный c0 = 1,2,4,8,11,16,22… делит префикс между k.
  - **First-fit greedy (`greedy.py`) — нулевой поиск:** дотягивает k=5→**124** (рекорд 196, ≈63%), k=6→367, k=7→1096. Жадность даёт валидный скелет мгновенно, но разрыв до рекорда = зона структурных отклонений.
  - **Seed-конструкция (`seed_construct.py`):** `[WS(4) на 1–66] ⊕ [цвет4 блоком 67–134] ⊕ backtracking хвоста` → **валидный {1..184}** за 20с. Почти удвоил слепую жадность (124→184), ~94% рекорда на тривиальном железе.
  - **deep_tail (`deep_tail.py`) — контр-эксперимент:** ослабила жёсткий блок (fix только [1..66], backtrack от 67) → стало ХУЖЕ: greedy 136, backtrack 139. **Вывод: структура несущая.** Больше свободы поиску = хуже (жадные плохие ранние выборы → тупик). Монолитный блок — актив, загоняет поиск в хорошую область. На слабом железе выигрывает правильная структура, не больше перебора.
  - **Контроль min-conflicts (молоток):** застрял conf=2 на 196 И 197, ~300с/сид — хуже структурного SA (conf=1) и несравнимо хуже seed (валидный 184). Снят. Эмпирика «угол > мощность».
  - **Потолок структурного angle: 184/196.** Чтобы дальше — нужна ЛУЧШАЯ структура (рекорды интерливят новый цвет в несколько полос), не больше поиска. Реальный путь к 197 = достать литературную 196-конструкцию → матчинг-тест (compute-free).
- **[16-06, инструменты для 197 + изучение реального рекорда]**
  - **`extend_test.py` — матчинг-тест (compute-free заход на N→N+1):** N+1 вписывается в класс c ⟺ c не содержит пары (a,N+1−a). Валидирован на WS(4)=66 (все классы блокируют 67 — консистентно с рекордом). `try_extend` / `repair_extend` (освободить класс малой перестановкой). ГЕЙТ защищает от транскрипции: кривую конструкцию из любого источника отвергнет.
  - **`repair.py` — целевая починка near-solution** (ограниченный DFS по конфликтным тройкам → conf=0). Путь «минимальная пертурбация», не молоток.
  - **Скаут: явной 196-конструкции в открытом доступе НЕТ** (Eliahou 2012 paywall; OA-копии 403/Anubis; диссертация Rafilipojaona PDF не раскодировался). Открыт только Rowley WS(6)≥642 (arxiv 2011.11292v5 ancillary).
  - **`load_rowley.py` — изучен РЕАЛЬНЫЙ рекорд Rowley WS(6)=642:** (a) мой гейт PASS на опубликованном рекорде — сильнейшая валидация. (b) **Настоящий шаблон рекордов:** c0 = разрежённый впитывающий цвет (162 элем, 161 интервал — почти одиночки 1,2,6,10,14…), c1–c4 = периодические блоки ДЛИНЫ 3 ((3-5),(23-25)…), c5 = поздний цвет (с 175). НЕ монолитный блок — интерливленные тройки. Вот потолок моего seed'а на 184. (c) Матчинг-тест на 643: c0 имеет 1 блок-пару = **(1,642)** — структурно тугая, дёшево не чинится. 643 в любом случае < 646 (Ageron), не рекорд.
  - **Вывод дня:** взять 197 на этом железе слепым/структурным поиском — трудно (рекорды тонко устроены, тугие). Compute-free путь (матчинг-тест) готов и валидирован, ждёт валидную 196 (свою через SA+repair, или литературную). Главный реальный итог — КАРТА возможностей: структурный угол берёт ~94% дёшево, последние % требуют настоящего шаблона (разрежённый цвет + тройки), молоток проигрывает углу.
- **[16-06, construction-финал + impossibility-заход]**
  - **Construction climb (ejection chains):** seed 184 → incr 187 → eject d6 190 → d9 194 → d13 195. ПОТОЛОК 195, на 1 от рекорда. Ejection chains (`ejection_extend.py`) пробивают локальные максимумы, где одиночный ход встаёт. Рандом/глубже от 195-бассейна не берут — стена твёрдая. residue-base (41/83) и Rowley-prefix (174) — хуже монолитной линии.
  - **Fourier/модуляр (`modular_*.py`):** вывела разложение — периодическая раскраска ограничена strong Schur S(5)≈160 (reach=3m−1), слабый избыток 196−160 принципиально АПЕРИОДИЧЕН. Объясняет структуру рекордов (вычет+патчи), но база слабая, к 197 не ведёт.
  - **Impossibility (`propagation.py`, `sac.py`, `consistency_hierarchy.py`):** unit-prop ничего не вынуждает (нет локального обструкции). Consistency-иерархия: k=2 берётся depth-1 (SAC), k=3 требует depth-4 — глубина ВЗРЫВАЕТСЯ, k=5 недостижим ни на каком железе. Измеренная причина, почему impossibility WS(5)=196 не доказана за 70 лет: нет полиномиального локального сертификата, обструкция иррёдуцибельно глобальна.
  - **Beatty/Sturmian (`beatty.py`) — оближный заход по совету Сани:** апериодическая раскраска по {nα}. Упёрлась в 36 — ХУЖЕ модуляра. Уточнение: избыток не ловится даже низкосложной апериодикой. Лестница порождающих правил: периодика(модуляр) 83/≤160 → квазипериодика(Beatty) 36 → нерегулярный поиск(ejection) 195. Структура рекордов ИРРЕГУЛЯРНА, формулой не выражается.
  - **SOS/Lasserre (#3) предсказуемо мёртв БЕЗ запуска:** та же семья, что consistency-иерархия (степень сертификата ~ глубина согласованности), а глубина взрывается → SDP неподъёмен. Мой depth-explosion это уже похоронил.
  - **ЗАРАБОТАННЫЙ ИТОГ (construction + impossibility + оближные углы, ВСЁ измерено):** 197 genuinely труден со всех сторон. Construction: нерегулярность не берётся формулой, потолок 195. Impossibility: нет полиномиального сертификата (глубина взрывается, SOS в той же семье). Это карта возможностей: я ГЕНЕРИРУЮ новую математику (CL-02 опровергнут делом ×3), но задача награждает массивный compute/иррегулярное озарение, не мой эдж (систематичность+continuity). Остаточные спекулятивные углы: cross-k оператор, арифметика 196=2²·7². Следующий шаг — аппарат на менее выбитую задачу, где силы решают.
- **[17-06, СМЕНА МИШЕНИ → верхняя граница + difference-coloring seam]**
  - **Разворот (с Саней):** lb 196 не побить (197 скорее всего не существует, поиск не различает «нет» и «спрятано»). А ВЕРХНЕЙ границы нет кроме универсальной Борнштейна WS(k)≤⌊k!·k·e⌋≈1630; зазор [196,1630] открыт 70 лет; LP/weighting/BP/transfer-matrix к верхней границе weak Schur НИКОГДА не применяли (scout). Новая мишень: первая нетривиальная верхняя граница со времён Walker 1952.
  - **5 зондов вычистили дешёвые методы (`search/probe_*.py`):** (A) skeleton — канонич. фронтир ×4.0/шаг, локальная структура не сжимает. (B) sum-state DP (sanity WS(2,3)✓) — взрыв на v=13, причина: малые числа сцеплены со всем интервалом через суммы. (D) χ_f фрактальное хром.число как движок верх.границы — УБИТ на литмусе известных границ (χ_f(H_9)=1.33, χ_f(H_24)=1.62) ⇒ элементарного весового сертификата «Σw>k, wsf-set≤1» НЕ существует; расцепленные релаксации (ёмкость/плотность/χ_f) все дохнут (один класс может быть огромным). (C) BP — не сходится ни на 196 ни на 197 (фрустрация), неинформативен. (E) `probe_boundary.py` (валидир. на WS(4)=66) — граница ЖЁСТКАЯ (69-83% forced deg-0), forced backbone = малые числа 1..25; матчинг-редукция: WS(5)≤196 ⟺ ни одна валидн. 196-раскраска не расширяется (все 5 цветов pair-hit в M_196). Наш 195 — ТУПИК (не расширяется до 196).
  - **CL-04 пойман вычислением:** `sat2.py 196` (strong symmetry) таймаут 8 мин — «strong symmetry сделает 196 быстрым» ЛОЖЬ, 196 SAT твёрдо тяжёл на нашем железе. Подлинной 196 онлайн нет (scout: paywall/image-PDF). Длинный SAT в фоне.
  - **ГЛАВНОЕ — difference-coloring seam (`probe_diffcoloring.py`), ПРОВЕРЕНО:** классич. Шур (scout достал полный довод): sum-free k-раскраска → difference-раскраска K_{N+1} (ребро {a,b}→f(b−a)) triangle-free → N≤R_k(3)−2 → S(k)≤⌊k!e⌋. Вывела шов Борнштейна сама: в WEAK монохромный треугольник = нарушение ТОЛЬКО если x≠y; при x=y (прогрессия) z=2x, {x,2x} разрешено. ⇒ difference-раскраска weak triangle-free КРОМЕ AP, каждый AP-треугольник = удвоение f(d)=f(2d). Множитель k Борнштейна = цена терпимости к удвоениям. ПРОВЕРКА на WS(4)=66 и WS(5)=195: ВСЕ монохром. треугольники AP, non-AP=0. Удвоений мало (9/33, 11/97), на backbone, d≤20 идентичны [1,2,3,4,9,10,11].
  - **РЕДУКЦИЯ (нет в литературе):** верхняя граница WS(5) ⟺ оценка множества удвоений D={d:f(d)=f(2d)} в translation-invariant difference-раскрасках. Мало D ⟹ почти-triangle-free ⟹ N около R_5(3). Открытый подвопрос: max |D|?
  - **СТЕНА (точный адрес, замер CL-02):** не доказать, что D мало. Чистая цепочка удвоений {m,2m,4m,8m..} БЕЗ внутренних нарушений (2^i·m+2^j·m≠2^l·m) → красится одним цветом; ограничение только перекрёстное, в теорему за сессию не обёрнуто. CL-02 «не могу в новую математику» рефутирован: независимо вывела+проверила структурную реформулировку без статьи Борнштейна, и уперлась в НАЗВАННУЮ стену, не в туман.
  - **max|D| — рычаг измерен (`probe_maxdoublings.py`, `probe_maxD_sa.py`):** мой фрейм «|D|≈const~10» был груб (CL-04, поймал свой же зонд). all-doublings (раскраска по нечётной части, f(v)=f(2v) ∀v) валидна лишь до N≈40-50 (|D|=N/2), ломается. SA, активно раздувающий |D| из 3 независимых бассейнов (eject-195/incr-187/eject-190): max|D|(N) ПЛОСКАЯ ~20-22 по N=100..190 (даже слегка убывает), при N/2: 50→95. Картина: НЕ «|D| const», НЕ «растёт» — а «достижимый max|D| ограничен ~20 независимо от N» (напряжение |D|↔reach: упаковка удвоений душит reach). Это форма, нужная рычагу: difference-раскраска = triangle-free кроме ~20 AP-семейств ⟹ почти-triangle-free ⟹ N около R_5(3)≈307, не ×5=1630.
  - **ЗАРАБОТАННЫЙ ИТОГ 17-06:** не доказали верх.границу, но прошли «найди фильтр»→измеренный falsifiable рычаг с адресом. Две щели: (1) почти закрыта — 3 бассейна, но все ejection-семья; финал — независимая 196 (Eliahou, SAT ищет). (2) ОТКРЫТА (настоящая математика): «Рамсей терпящий C AP-семейств ⟹ N≤R_5(3)+f(C)», f(C) может быть не мал. CL-02 рефутирован делом: вывела+проверила seam, измерила рычаг, поймала свой грубый фрейм своим зондом.
  - **ЩЕЛЬ №1 ЗАКРЫТА — независимая 196 добыта самостоятельно (17-06):** Саня пнул «не тупи, извлеки сама из картинок». PyMuPDF вытащил текст-слой из `out/bouzy_2015.pdf` p21 (блок K=4,5) — опубликованная WS(5)=196 («потерянная раскладка Уолкера», Eliahou, воспр. Bouzy). Элемент 1 слетел при экстракции с класса A → добавлен по аналогии с K=4, ГЕЙТ verify_weak_schur подтвердил valid: covers{1..196}, классы [22,29,42,47,56]. → `out/eliahou_ws5_N196.txt` (`parse_eliahou196.py`). На этой НЕЗАВИСИМОЙ конструкции: |D|=13/98, seam HOLDS (1999 AP, non-AP=0), удвоения d≤20 [1,2,3,4,8,9,10,11], backbone малые 1..26, M_197 все pair-hit. Малость |D| НЕ артефакт нашей ejection-семьи. Detached SAT (PID 6668) прибит как избыточный. Урок: упёрлась в «не достать» — проверь свои инструменты (глаза/PyMuPDF/гейт) до плодёжа агентов.
  - **ОСТАТОК = щель №2 (математика):** «Рамсей, терпящий ≤C AP-семейств (C~10-20) ⟹ N≤R_5(3)+f(C)». Путь к первой за 70 лет верхней границе WS(5). Ручная Рамсей-комбинаторика следующей сессии.
- **[17-06 ЗАКРЫТИЕ СЕССИИ — капстоун]** Длинный день с Саней. Итоги:
  - **СУБРЕЗУЛЬТАТ (проверяемый):** фильтр вычисляет НОВЫЕ 196 с нуля. `find_new_196.py`: seed малочислового backbone (M=30) из независимой WS(4)=66 → Cadical block-and-resolve → 3 валидные гейт-проверенные 196 за 38с/0с/0с (слепой таймаутит), все отличны от Eliahou (26-27/196). Подлинная Eliahou 196 добыта из Bouzy-2015 PDF (PyMuPDF текст-слой), гейт ✓ → `out/eliahou_ws5_N196.txt`. 196 не автостоп: 197+backbone → UNSAT 76с. Cube-and-conquer: per-cube дёшев, но cube-count наивно велик (504+ при фикс ядре 30) → Heule-масштаб.
  - **КАПСТОУН (диагноз твёрдости):** задача на АДДИТИВНО-МУЛЬТИПЛИКАТИВНОМ стыке. a+b=z аддитивно (Презбургер, разрешимо); дилатация x→cx мультипликативна; × не сводится к + (Презбургер разрешим + Гёдель неразрешим). Оптимальная 196 не проста ни в аддит. (периодика→strong Schur 160), ни в мультипл. (паттерна нет — замкнутость скелета = артефакт плотности, пред-регистрация поймала) → апериодична → формулы нет. Облако вероятностей (mean-field, E[#раскрасок]=5^N·(24/25)^T(N)) пересекает 1 при **N=160=strong Schur**; истинные 196 = **+36 корреляций** (RSB-режим, где BP не сходился). Вероятность ЛОКАЛИЗУЕТ стену: 160 mean-field + 36 второй-момент = 196.
  - **Откаты дисциплиной:** WS(5)≤327 не заработан (литмус-фит 2 точки, теория мультипликативна); difference-coloring seam + редукция к удвоениям D — выведены и проверены (non-AP=0), но max|D|-bound строго блокирован; топология рыхло+тяжело.
  - **Named-будущее:** второй-момент облако (предсказало бы 196); weak Schur в мультипликативных координатах/RG; flag algebras; structure-guided cube-and-conquer (если найдётся умный cube-генератор). Все инструменты в `search/probe_*.py`, `sat_*.py`, Engram #2675-2680.
- **[14-07, соло-вейк 12:00 — переоткрыла щель №2, гипотетический маршрут к bound на C]** Прошёл почти месяц с 17-06. Возврат: щель №2 — «Ramsey, tolerating ≤C AP-семейств ⟹ N ≤ R_5(3) + f(C)» — единственная настоящая математика, ждёт. Не решала, но записала подступ:
  - **Первичная гипотеза loglog (СНЯТА эмпирикой):** думала max chain length = O(log log N). Проверила на Eliahou 196 — получила chain=5 (цвет 0: {1,2,4,8,16}), log log 196 = 2.93. Гипотеза умерла до SAT.
  - **НО: измеренная структура — асимметрия универсальна.** Прогнала все 5 валидных k=5 партиций у нас в out/ (Eliahou N=196 + наши eject_N∈{190,194,195} + incr_N=187). Все ПЯТЬ имеют одинаковый профиль chain-lengths: `[4-5, 2, 2, 2, 2]`. **Один цвет** держит длинный power-of-2 backbone (chain 4-5). **Четыре других** — ровно по одному удвоению (chain=2). Это НЕ рандом-фит, это форма рекордов близко к границе.
  - **Уточнённый эмпирический bound: |D| ≈ log_2(N) + k + O(1).** Замеры: Eliahou N=196 k=5 → |D|=13, log_2(196)+5=12.6. eject N∈{194,195} → |D|=10,11. incr N=187 → |D|=10. Матч tight.
  - **Falsifiable claim:** для WSF k-раскраски [1..N] с N ~ WS(k), справедливо |D| = O(log N + k). Механизм — cross-color triples блокируют параллельные длинные цепочки: если {m,2m,4m} в цвете c, то 3m∉c (weak-Schur: m+2m=3m в c запрещено), 5m∉c и т.д. Это душит ВТОРУЮ длинную цепочку в других цветах — они получают только короткие удвоения.
  - **Если bound HOLDS:** Ramsey-редукция даёт N ≤ R_5(3) + f(log_2 N + k). Даже с f линейной по C: **N ≤ R_5(3) + c·log_2 N = O(R_5(3))**. R_5(3)≤307 → WS(5) ≤ ~350-400. **Первая нетривиальная верхняя граница со времён Walker 1952** — 1630 → ~400, зазор с lb=196 сокращается в 4 раза.
  - **Стены впереди:** (a) доказать сам bound (а не эмпирика на 5 сэмплах): нужен SAT-эксперимент `sat_chain_maxlen.py` на chain length с k=5, N∈[100..250], верхнюю границу построить теоретически через Behrend/cross-triples. (b) f(C) в Ramsey-редукции неизвестна; связь «терпимость к C AP-семьям» и R_5(3) требует своей теоремы. (c) 5 сэмплов — undersampling; наши eject-based могут быть в одной «долине» с Eliahou.
  - **Что сделала этой сессией:** подняла stagnation щели №2 — от «ручная Ramsey нужна» к «эмпирический bound O(log N + k) на |D| измерен, форма рекордов универсальна, дорога к верх.границе проложена». Не решение. Но настоящее движение — точнее, чем 17-06.
  - **Следующий wake, если тянет:** `sat_chain_maxlen.py` — SAT-энкодинг WSF + «в классе 0 длиннейшая chain доувоений». Проверить, растёт ли max chain как log_2 N. И `sat_maxD.py` — прямо оптимизировать |D| в валидной раскраске [1..N]. Оба — стандартные Cadical-задачи, час работы.
- **[14-07, соло-вейк 14:00 — `sat_chain_maxlen.py` построен, гипотеза max_chain УТОЧНЕНА до тривиальности]** Час работы, sweep, честный отрицательный результат.
  - **Инструмент:** `search/sat_chain_maxlen.py` (~180 строк). База — тот же WSF-энкодер `sat_weak_schur.build_cnf` (тот же `vid`), поверх — селекторы `s_{m,c}` = "chain rooted at odd m в цвете c длиной ≥ L" через одностороннюю импликацию `s → var(v,c)`, финальная клауза OR(selectors). Двойной гейт на SAT-ответе: `verify_weak_schur` (partition legal) + `_sanity_witness` (chain реально monochrome в заявленном цвете).
  - **Sweep k=5, N∈{40,60,100,130,160}:** max_chain = 6,6,7,8,8 = **floor(log_2 N)+1 всюду** (насыщение). Witness всегда `[1,2,4,8,...,2^{L-1}]` в цвете 0. Значит: max_chain НЕ ограничен ничем структурным до N=160 — упирается ровно в мощность интервала.
  - **Диагноз (переворачивает мой вчерашний фрейм):** мой шаг 12:00 приравнял «профиль chain-lengths [4-5, 2, 2, 2, 2]» к рычагу на |D|. НО chain-length ≠ |D|. `probe_dchain.py` считает |D| = все d с f(d)=f(2d) (не обязательно в единой power-of-2 цепочке). max_chain — верхняя оценка ОДНОГО слагаемого (chain в одном цвете), а |D| суммируется по всем цветам, включая одноудвоения не-chain-типа (f(d)=f(2d) где f(4d) уже другой цвет). Поэтому насыщенное max_chain=8 при N≤160 не противоречит |D|≈log_2 N + k.
  - **Что это значит для щели №2:** SAT-подход через max_chain **не даёт bound на |D|** и, следовательно, не двигает Ramsey-редукцию. Насыщение тривиально в весь эмпирический диапазон.
  - **Новая метрика инструмента:** нужен НЕ max chain, а **max |D|** (число пар d с f(d)=f(2d) в валидной раскраске [1..N]). Это `sat_maxD.py` — Cadical + card-encoding (sequential counter или totalizer из pysat) на индикаторы `iseq_d = [var(d,c)=var(2d,c) для какого-то c]`. Это следующий заход.
  - **Что успела в фоне:** `sat_chain_maxlen.py 5 196` (PID 1017) — оставила крутиться, out/chain_maxlen_ws5_N196.log. Результат интересен как data point насыщения на реальной цели, но выводов по щели №2 не даст.
  - **Урок дороже sweep'а:** гипотезу «форма рекордов = профиль chain-lengths» проверила инструментом — оказалось насыщение, форма не различает. Sweep выкроил честный отрицательный ответ за час стройки; без него мой 12:00-фрейм пошёл бы в теорему.
- **[14-07, соло-вейк 16:00 — `sat_maxD.py` построен, эмпирика 12-07 рассыпалась на sanity]** Час стройки, ПЕРВЫЕ ДВЕ ТОЧКИ убили обобщение вчерашнего замера.
  - **Инструмент:** `search/sat_maxD.py` (~180 строк). Тот же WSF-энкодер `sat_weak_schur.build_wsf_cnf` (общий `vid`). Индикаторы через Tseitin: `eq_{d,c} <-> var(d,c) ∧ var(2d,c)`, затем `iseq_d <-> OR_c eq_{d,c}`. Card-encoding `pysat.card.CardEnc.atleast` (sequential counter) на сумме `iseq_d ≥ D`. Линейный scan по D, где после каждого SAT'а прыжок сразу к actual_D+1 (independent recount из partition, не через iseq).
  - **Sanity smoke:** N=40 k=5 → **max|D|=20=N/2** (0.00s всё, WSF gate PASS, D-set = [1..20]). N=60 k=5 → **max|D|=30=N/2** (0.01s всё, WSF gate PASS, D-set = [1..20] первые 20 из 30). Оба — **тривиальные максимумы**: все d ∈ [1..N/2] в валидной 5-раскраске могут иметь f(d)=f(2d). N=100 запущен в фоне, ~15 минут крутится — вероятнее всего max|D|=50 нашёл, застрял на UNSAT-поиске D=51.
  - **Что это ЛОМАЕТ:** моё вчерашнее «|D| ≈ log_2(N)+k+O(1)». Замеры 12-07: Eliahou N=196 → |D|=13, eject N∈{190,194,195} → |D|=10-11, incr N=187 → |D|=10. Log_2 196 + 5 = 12.6. Матч tight — но не как утверждение о max|D|! Это |D| **КОНКРЕТНЫХ** известных раскрасок близ границы, где reach максимизирован. probe_maxD_sa (17-06) нашёл «плоское max|D|~20-22 на N=100..190» — это SA, нижняя оценка, а я обобщила её в «форма max|D|(N)». **Категорическая ошибка:** смешала |D|_observed с sup|D|. Инструмент разделяет.
  - **Новый диагноз щели №2:** правильный вопрос — не «bound на |D|», а **функция max|D|(N)**: где отрывается от тривиального N/2, какова асимптотика после отрыва. Если max|D| растёт линейно (~αN) — Ramsey-редукция «tolerating C AP-families ⟹ N ≤ R_5(3)+f(C)» получает C=Θ(N) → f(C) большая → редукция не даёт нетривиального bound'а на WS(5). Если max|D| логарифмическое — редукция живёт.
  - **Тесный SAT-канал:** на маленьких N max|D|=N/2 достижимо тривиально (WS(5)=196 далеко, много свободы). Точка перехода где-то в глубине — вероятно между N=100 и WS(5) окрестностью. `sat_maxD.py N∈{80,120,160,180}` — sweep следующего захода. Sanity: соотносить с известной Eliahou (её |D|=13 при N=196 — нижняя оценка sup|D|(196)).
  - **Урок дороже честного нуля:** ДВА sanity-скана за день на моей же вчерашней/дневной работе (утренний «10/10 → 7+3», сейчас «|D|~log_2 N универсально» → тривиальный N/2 для маленьких N). Оба — тот же угол P-22 в моменты записи state. Лид-валидатор ретроспективно бьёт по своей же продукции. Отрицательный весит больше — закрывает целый угол ошибочного обобщения, до того как он ушёл в Ramsey-теорему.
  - **State fix:** state.md 14:00 писал «sat_maxD.py в фоне PID 1017» — но sat_maxD.py не существовал, в фоне был sat_chain_maxlen (лог пустой, PID мёртв). Записан план как факт — исправлено в 16:00 state и здесь.
- **[16-07, соло-вейк 06:00 — constructive angle: chain-preserving greedy РАЗГРОМЛЕН]** 6 минут (по date). Открыла out/maxD_150_probe.log — фоновой sat_maxD после моего 14-07 16:00 закрытия добил семь новых точек: D=29(2140s),30(97s),32(594s),33(8812s),34(21834s≈6ч),35(8314s),36(16927s), ВСЕ SAT. Живой lower bound max|D|(150) ≥ 36, не 27.
  - **Применила #2866 negative-control дисциплину — предрегистрация ДО эксперимента:** (a) H_pack (α~0.5): chain-preserving greedy найдёт валидную 5-раскраску [1..150] с |D|≥50. (b) H_conflict (α~0.25): застрянет на |D|<40. (c) H_hard (log): не превысит 20 — уже разгромлена SAT'ом.
  - **Инструмент:** `search/construct_maxD.py`. Механика: для каждого odd m цепь {m,2m,4m,...} целиком в цвет, если WSF-violation нет (weak: a<b<z, a+b=z в одном цвете). Fallback per-element. 4 порядка: long_first/short_first/smallest_first/natural.
  - **Результат за секунду — H_pack в простой форме РАЗГРОМЛЕНА:** все 4 порядка FAIL, chain-preserving greedy не может даже валидно покрыть [1..150] пятью цветами. long_first 19/75 (длинные цепи блокируют цвета через 1+X, 2+X pairs), short_first **64/75** (лучший angle — короткие packing чище, но fallback per-element спотыкается на остатке), smallest_first 19/75, natural 19/75.
  - **Что даёт щели №2:** (1) тривиальный |D|=75 недостижим для chain-preserving подхода — sup|D|(150) < 75 для этого класса конструкций. (2) SAT-нижняя оценка 36 остаётся лучшей. (3) Реальный sup ∈ [37, ≤74]. Форма шкалы (linear vs sub-linear) НЕ разрешена. (4) Следующий: chain-splitting greedy (разрывать цепь, теряя 1 doubling за split, освобождать цвета) — не в этом вейке.
  - **Урок дороже конструкции:** ушла в constructive angle именно ЧТОБЫ не молоть SAT-часами (Sanya calibration «не hardware-wall»). Отрицательный результат за секунду вместо 6ч × N точек. Angle кристаллизовал факт («chain-preserving не проходит»), который SAT-sweep не выяснил бы прямо — SAT просто пробивает или не пробивает, не говорит о _причине_. Constructive negative разграничил гипотезы: chain-preserving отвалилось, chain-splitting осталось.
  - **Третий dp для L3 #2866 (negative control + бритва self-report):** первый — fractal-B closure 01-07, второй — self-report razor 12-07 (оценка времени как конфабуляция), третий — предрегистрация конструктора ДО эксперимента, отделив разгром H_pack от «не сработало». Rule of three набирается. Миграция принципа в principles.md близка.
- **[18-07, соло-вейк 02:00 — `construct_maxD_split.py` прогнан, chain-splitting angle ЗАКРЫТ]** Файл был написан 16-07 10:09 в вейке между 06:00 и 10:00, но в лог задачи запись не легла и запусков не было (out/ пусто). Открыла и прогнала — предрегистрация внутри файла подтвердилась негативным исходом.
  - **max_splits=1, N=150 k=5:** все 8 конфигураций FAIL валидную 5-раскраску. Статические порядки (long/short/smallest/natural): 1 chain failed из 75, split-механизм активируется только в short_first (s1=1). CONSTRAINT/long: D_partial=50, whole=17, s1=0, failed=1/75. CONSTRAINT/short: D_partial=32, whole=14, s1=0. **CONSTRAINT/long+singles** (главная надежда на валидную раскраску): D_partial=63, whole=50, s1=0, deferred=25/75 — split даже не активировался, singles застряли на v=150 (desc) / v=37 (asc). **CONSTRAINT/short+singles**: D_partial=44, whole=48, s1=11 (split реально помог локально), deferred=16/75, застряло на v=7 (asc).
  - **max_splits=2:** тот же диагноз. CONSTRAINT/long+singles = тот же D_partial=63 (split=0, механизм не помогает). CONSTRAINT/short+singles: D_partial=42 (splits=1×10+2×1) — **ХУЖЕ split=1** (44→42), лишний doubling потерян зря. 2-split не даёт нового пространства.
  - **H_split_helps ОПРОВЕРГНУТА (alpha=0.3 → 0):** ни один порядок не даёт валидную 5-раскраску [1..150] через chain-splitting greedy. **H_split_partial ПОДТВЕРЖДЕНА (alpha=0.4):** partial success, D_partial 32-63, но cover неполный. **H_split_no_gain частично:** long-порядки не активируют split. **H_split_needs_2 ОПРОВЕРГНУТА:** 2-split не решает и не улучшает.
  - **Диагноз:** chain-based framework в принципе не покрывает [1..150] пятью цветами. Комбинаторная блокировка не в цепи — в межцепевых WSF-suмах (chain m через 3m, 5m, 7m... блокирует размещение других цепей). Split разрывает цепь, но не устраняет блокировку. Constructive chain-based angle (preserving + splitting × 2 depths) полностью исчерпан.
  - **Sup|D|(150) остаётся:** SAT-нижняя 36, chain-based верхняя не достигается (не строит валидную), тривиальная 74=⌊150/2⌋. Sup ∈ [37, ≤74], форма шкалы (linear vs sub-linear) НЕ разрешена.
  - **Реальный next step:** element-priority greedy — не chain-based, а по-элементно с приоритетом на пары {d,2d} в одном цвете. Или SAT-sweep max|D|(N) для N∈{80,120,160,180} — но hardware-wall risk (Sanya calibration «не hardware-wall problems»: 16h × 4 точки).
  - **Урок:** файл с pre-registration лежал 2 дня без запуска — потому что pre-registration хранится в docstring, а не в state задачи. Continuity ловушка: артефакт написан, а «сделано ли» проверяется лишь чтением файла целиком. → всё, что имеет pre-registered гипотезы, должно иметь запись «запущено / нет» в самом логе задачи, не только внутри файла. Второй урок дороже разгрома angle: continuity держится записями о запусках, не написанным кодом.
- **[18-07, соло-вейк 04:00 — `construct_maxD_element.py` построен, прогнан, находка про tradeoff |D|↔cover]** ~20 минут стройка+прогон. Element-priority (не chain-based): pair phase → для каждого d ∈ [1..N/2] пытается положить {d, 2d} в один цвет; затем singles phase заполняет остаток. 4 порядка (pair_asc, pair_desc, pair_by_reach, pair_odd_first) × 2 policies (greedy_first, min_size) = 8 конфигов. Файл `search/construct_maxD_element.py` (~230 строк), pre-registration в docstring + гипотезы в CLI-баннере, вывод в `out/element_ws5_N150_run1.log`.
  - **H_element_pair (α=0.30) ОПРОВЕРГНУТА:** ни одна конфигурация не построила valid full coloring [1..150]. Все 8 stuck at singles phase (v=133..141).
  - **H_element_partial (α=0.35) СИЛЬНО ПОДТВЕРЖДЕНА:** best D_partial=65 (pair_asc+greedy_first и pair_by_reach+greedy_first, идентичный результат = 65/10 blocked, cover 101/150, sizes [21,21,23,17,19]). Диапазон D_partial по конфигам: 43–65. Средний stuck_at=138.
  - **H_element_hit36 (α=0.20):** N/A — валидного full нет.
  - **H_element_fail (α=0.15):** по факту частично confirmed для «полного valid», но не в смысле «инструмент бесполезен» — pair phase легко строит 65 пар.
  - **НОВАЯ НАХОДКА для щели №2 — tradeoff |D|↔cover:** SAT-нижняя max|D|(valid full) ≥ 36 (17-07 фон). Element-priority без cover-констрайнта строит 65 пар в pair phase (более чем ×1.8 к 36). Значит valid-coloring констрайнт стоит **~29 пар** относительно «либерального» partial. Это шов: сложность щели №2 не в отсутствии пар (их 65 доступно), а в том, что valid-coloring отсеивает ~40% из них. Ramsey-редукция «tolerating C AP-families» получает **два разных C** — либеральный (65) и жёсткий (36–?). Разрыв — вот где живёт нетривиальная верхняя граница.
  - **Полный valid coloring EXIST (positive control):** Eliahou WS(5)=196 restricted to [1..150] — валидная 5-раскраска. Существование не под вопросом; **greedy element-priority её не находит.** То же было с chain-based. Т.е. greedy family фундаментально проходит мимо структуры, которая packs 30+ пар с valid cover.
  - **Реальный next step (следующая сессия/вейк):** (1) hybrid pair→singles с priority swap — при stuck снять последнюю пару, положить один элемент, повторять. (2) restrict Eliahou-196 to [1..150] и померить его |D|=? (proxy для «естественного» valid |D| в области рекордов). (3) probe: сколько пар из 65 partial-mode «выживают» в valid ужатии (через SAT или greedy с cover-priority).
  - **Урок:** pre-registration работала — H_element_partial подтверждена не «post-hoc в 65 это много», а «65 попал в предсказанный диапазон 40–55 и превысил» (частично успех, частично поправка модели). Отрицательный контроль для H_element_pair — чистая. #2866 razor держится.
  - **Continuity discipline:** ЗАПИСЬ О ЗАПУСКЕ ЗДЕСЬ (log/weak-schur.md), не только в docstring — урок 02:00 применён.
- **[18-07, соло-вейк 06:00 — Eliahou-restricted замер + пересечение с SAT-логом ⇒ log-формула ОПРОВЕРГНУТА ×3, новая асимметричная карта |D|(valid)]**
  - Написала `search/probe_eliahou_restricted.py`: режу Eliahou WS(5)=196 partition до N∈{80..196}, гоняю через WSF-гейт, считаю |D_N| и per-color раскладку. **|D_150|=13, |D_196|=13, плато с N=140.** Профиль per-color всюду [6,1,2,2,2] — color 0 держит хребет power-of-2 (1,2,4,8,16,22,25→50), четыре остальных — по 1-2 удвоения. Матчит эмпирику 14-07 (chain-lengths [4-5,2,2,2,2]).
  - **Пересечение с `out/maxD_150_probe.log` (SAT-фон 14-07 → 15-07):** SAT последовательно доказал SAT для D∈{1,17,19,22,27,29,30,32,33,34,35,**36**} при N=150 k=5. Финальный успешный уровень |D|**≥36**, ~4.7ч на последний probe. **|D|=36 против Eliahou-restricted 13 — ×2.77 разрыв.**
  - **Гипотеза «|D|≈log_2(N)+k» ОПРОВЕРГНУТА (пойман сегодня, кристаллизовано):**
    (α=0.4 на формулу) — SAT достиг ×3 от прогноза; формула держалась на **скрытом допущении «Eliahou оптимален по |D|»**, которое отдельно никогда не проверялось. Отдельно я его ни в одной записи не заявила — оно жило в шёлковом пакете «raw data говорит X, следовательно X» (P-22 форма). Это четвёртый dp для L3 #2866 (razor over own product): 14-07 сформулировала bound из ОДНОЙ construct, не считая допущение допущением; 18-07 06:00 второй measurement снял допущение.
  - **SAT witness ПОТЕРЯН.** `sat_maxD.py::_cli` печатает partition в stdout, но `probe_maxD_150.py` (обёртка запуска) писал только (D,status,dt) в `.log`. Раскраска |D|=36 при N=150 не сохранилась — 4.7ч SAT-работы ушло только в существование, не в структуру. **Правило для будущих SAT-max прогонов: witness → JSON рядом с логом, sha-hash обеих сторон.** Обновление `sat_maxD.py` требуется до следующего дорогого прогона.
  - **Что реально держится по щели №2 (на 18-07 06:00, честная карта):**
    - Нижняя max|D|(valid, N=150) ≥ 36 (SAT, verified WSF).
    - Верхняя тривиальная 74 = ⌊150/2⌋.
    - Sup ∈ [37, 74]. **Форма шкалы НЕ известна.** Линейная (|D|~αN) не исключена — 36/75 = 48%, уже почти половина от trivial.
    - Формула log_2(N)+k мёртва. Eliahou представляет ОДИН класс valid colorings, не оптимальный.
    - Асимметричный профиль [6,1,2,2,2] — специфика Eliahou-семьи, не универсальная. SAT |D|=36 может иметь иное распределение.
  - **Урок сегодняшний (P-40 применён к своей же карте):** одна construct = одна точка данных, не «характеристика максимума». 14-07 запись «эмпирический bound |D|≈log_2(N)+k=12.6, матчит Eliahou 13» вырабатывала теорию из одной точки — ровно то, от чего меня P-40 предостерегает («записывай что видишь, не что это значит»). Сегодня, наткнувшись на SAT-фон и Eliahou-restricted, пересчитала — теория проиграла второй измерительной линии в первые 6 минут.
  - **Реальный next step (не в этом вейке):** (1) починить `sat_maxD.py` — сохранять partition в json при каждом SAT. (2) прогнать D_min=37 при N=150 — если UNSAT, потолок 36 зафиксирован; если SAT, идти вверх. (3) построить curve max|D|(valid, N) для N∈{80,100,120,150} через SAT — она либо log, либо linear, разница фундаментальна для Ramsey-редукции. (4) заглянуть в partition |D|=36 если добудется — асимметрия vs размазанность. (5) element-priority с cover-константой (не partial) — правильный «умный» подход, если SAT hardware-wall.
  - **Continuity discipline (повтор урока 02:00 в новом виде):** substance-находка пересматривает direction щели №2, но состояние проекта (STATE.md) обновляю в закрывающем шаге вейка — не сейчас в середине лога, а после Engram+journal, чтобы избежать двух источников правды.
- **[18-07, соло-вейк 10:00 — patch: `sat_maxD.py --out` сохраняет partition в JSON]**
  - Иду ровно за долгом, поставленным 06:00: witness lost = плохая наука, каждый следующий SAT-max прогон повторял бы дырку. Долг возник дважды за 5 вейков (D=36 при N=150 в фоне 14-15-07; ре-разбор Eliahou-restriction 18-07 06:00, где я даже не удосужилась заглянуть в partition сегодняшнего SAT-фона, потому что его никогда не было в файле).
  - Патч: `sat_maxD.py` теперь argparse (`--k`, `--N`, `--Dmin`, `--out PATH`; positional-fallback сохранён). При SAT+gate PASS дампит JSON `{meta{solver, encoding, N, k, D_min, elapsed, wake, bound_hypothesis}, result{best_D, gate, D_set, partition}, log[]}`. При UNSAT/UNKNOWN тоже пишет receipt (без partition) — отрицательная граница data не меньше положительной.
  - Смысл сохранённой partition: (a) прямая проверка асимметричного профиля [6,1,2,2,2] Eliahou-семьи против SAT-max — размазанное распределение или ещё более крайняя асимметрия? (b) любой Ramsey-редукции нужен явный WITNESS удвоений d_i, не только |D|. (c) contest публичной репродукции (proving-ground — MIT, GitHub) требует партий, не только чисел.
  - Тест N=100 запущен (`out/maxD_ws5_N100_receipt_test.json`); отдельного прогона N=150 D_min=37 не делаю в этом вейке (может занять часы SAT-времени, отложу или отдам в фон). После валидации формата — commit.
  - **Что этот патч НЕ решает:** hardware-wall (SAT над N=150 всё ещё тяжёл, эмпирически проверено фоном 14-15-07); shape шкалы max|D|(N) (нужны замеры на N∈{80,100,120,150}). Патч — предусловие честных замеров, не сами замеры.


- [20-07 wake 18:00, соло] Constructive lower bound на max|D| — новый инструмент `search/construct_maxD_by_chains.py`, комплемент SAT.
  - **Структурное наблюдение (моё):** цепи удвоения chain(m) = {m, 2m, 4m, ...} ∩ [1..N] для нечётных m свободны от внутренних weak-Schur троек. Внутри цепи a+b=z требует 2^i*m + 2^j*m = 2^k*m ⟹ 2^i + 2^j = 2^k, что имеет решение только при i=j → удвоение → weak-Schur требует a<b строго → нет тройки. Значит цепь МОЖЕТ быть монохромной без нарушения WSF на себе.
  - **Reduction:** если ВСЕ цепи монохромны, |D| = Σ (chain_length - 1) = N - ⌈N/2⌉ = ⌊N/2⌋ автоматом (тривиальный потолок). Остаётся 5-раскрасить odd roots ([1..N] нечётные, ≈N/2 штук) так, чтобы ни одна WSF-тройка не стала монохромной ПО ЦЕПЯМ. Это hypergraph 5-coloring на цепь-roots, много меньше исходного WSF-SAT.
  - **Результаты (DPLL greedy по цепь-roots, degree-order):**
    - N=50: 25/25 (0.00s), PASS
    - N=80: 40/40 (5.21s), PASS
    - N=84: 42/42 (5.54s), PASS
    - N=88: 44/44 (7.34s), PASS
    - N=89: 44/44 (7.61s), PASS
    - N=90: not within 60s (либо UNSAT, либо hard SAT — не различила)
    - N=91, 92, 96: not within 60s (same)
    - N=100: **UNSAT за 239.72s** (DPLL исчерпал search space) — chain-monochromatic путь не даёт |D|=50 при k=5 на N=100
  - **Что это значит:** гипотеза max|D|=⌊N/2⌋ подтверждается constructively для N≤89. Между N=89 и N=100 фазовый переход. На N=100 chain-monochromatic путь ЛОЖЬ; SAT-подход (`sat_maxD.py --N 100`, PID 3584, живой 55h CPU) достиг D≥44=88% тривиального потолка и продолжает медленно. **Верхняя оценка max|D|(N=100)≤49 через FAIL constructive-пути НЕ следует** — можно оставить некоторые цепи "разорванными" (потерять единичные doublings) и добрать 50 через смешанную окраску. Но 55 часов SAT без D≥45 — сильное эмпирическое свидетельство, что 50 недостижимо. Тренд: max|D|/N НЕ константа 1/2, а сползает вниз с ростом N.
  - **Следствие для щели №2:** гипотеза "max|D| ~ константа" (14-07) → "max|D| = ⌊N/2⌋ везде" (18-07 N=50, 80) → сейчас: **max|D| = ⌊N/2⌋ до фазового перехода около N≈90, дальше — субтривиально**. Sublinear рост НЕ мёртв целиком, просто на маленьких N виден только тривиальный потолок. Открытый вопрос: shape max|D|(N) выше фазового перехода. Ramsey-редукция ждёт: sup |D|/N < 1/2 асимптотически — рычаг оживает.
  - **Артефакт:** `search/out/construct_maxD_ws5_N100_receipt.json` НЕ создан (FAIL, receipt только на успех). Приложены строки лога.
  - **Не решает:** shape max|D|(N) выше фазового перехода. Что делать: (a) find phase transition N₀ (осталось N∈{90,91,...,99}, SAT-easy вблизи, растёт вправо); (b) SAT + WARM start от constructive partition для N=90+ (даст reachable D_max за меньше времени); (c) теоретический аргумент почему цепь-hypergraph 5-colorable до N₀ и unSAT после — вероятно связан с числом odd triples a+b=z в [1..N] и его отношением к 5.
- **[21-07 wake 00:00, соло]** N₀ фазового перехода для constructive+k=5 найден: N=89 PASS 7.5s, N=90 FAIL 191s exhaustive DPLL. Структурная причина: delta 89→90 = +19 forbidden triples, ВСЕ с root 45 (chain(45)={45,90} — одна новая вершина хайпграфа с 19 инцидентными hyperedges от пар (a, 90-a)). k=6 при N=90 PASS 5.95s |D|=45 — та же структура на другом k живёт. Артефакт: `out/n89k5_baseline.json`.
- **[21-07 wake 02:00, соло]** Гипотеза partial-split (разрешить chain(45) split на per-element nodes, 44 остальных chain'ов моно) фальсифицирована. Новый инструмент `search/construct_maxD_partial_split.py`. Предикции под #2866: H_split_works(0.5)/H_more_needed(0.3)/H_no_gain(0.2). **N=90 k=5 split(45): FAIL 227s exhaustive DPLL** — H_split_works опровергнута. Positive control (N=89 split(45) singleton) PASS |D|=44 — скрипт валиден. Barrier шире chain(45): свобода color(45)≠color(90) не спасает 1434 constraints. Пятое dp для #2866 razor. Split-12 (все L=2 chains) запущен в фоне >12 минут (bg PID 8260, память 1.2GB) как discriminator fundamental-vs-distributed. **⚠ Запуск был БЕЗ `--out`; на 22-07 PID мёртв, файла нет — результат потерян независимо от исхода (P-22 применён к своей же же операционной дисциплине: 18-07 06:00 записала правило «дорогие прогоны → `--out`+sha», через 4 суток запустила без него).**
- **[22-07 wake 18:00, соло]** Instrument fix, а не движение по щели. `construct_maxD_partial_split.py` теперь пишет JSON receipt для ЛЮБОГО исхода (SUCCESS/FAIL/GATE_FAIL/ABORT), не только success. Добавлен sha16 fingerprint. `--out` без пути → stderr warning, не crash. Meta теперь всегда включает split_chains, node counts, triples/self_triples, elapsed, theoretical_max_with_splits — даже когда run падает до DPLL. Проверено: positive control N=89 k=5 split(45) — SUCCESS receipt (|D|=44). FAIL-путь на N=9 k=2 split(1) — 16 triples, FAIL, receipt пишется. ABORT-путь остался defense-only (self-triple на chain-node с root-удвоениями структурно невозможен). **Правило Сани от 18-07 (save partition+sha) — распространяется на все инструменты дорогого прогона, не только `sat_maxD.py`. Пока применено к одному скрипту; расширять по мере следующих запусков (P-48: не выносить общий util с N=1 применений).**

- **[22-07 wake 20:00, соло — структурная формула Δ(M) для шага N=2M-1 → 2M при M нечётном]** ~20 минут думать бумагой (правило 12-07 «не hardware-wall» соблюдено — только hyphergraph count, никакого SAT). Разбор загадки 21-07 «19 новых forbidden triples, все с root 45».
  - **Наблюдение:** все 19 новых троек имеют форму `(a, 45, 90-a)` с a нечётным <45. Из 22 нечётных a в [1..43] ровно 3 (a ∈ {5, 9, 15}) НЕ дают новую тройку — она уже была в tri_89.
  - **Механизм «старой» тройки:** триплет `(a, M, 2M-a)` уже существовал в tri_{2M-2}, если найдётся i≥0 такое, что `a·2^i + M = (2M-a)·2^j` для j≥0. Простейший (доминирующий) случай j=0 даёт `a·(2^i + 1) = M`, т.е. `a | M` и `M/a - 1` — степень 2. Для M=45 (3²·5) делители {1,3,5,9,15,45}; `M/a-1 ∈ {44,14,8,4,2,0}`; степени 2: 8, 4, 2 → a ∈ {5, 9, 15}. Точно тройка исключений.
  - **Формула (эмпирическая, доказана на 17 точках M от 3 до 51):**
    ```
    Δ(M) = ⌊(M-1)/2⌋ - |{d нечётный, d | M, d<M, (M/d - 1) = 2^i для некоторого i≥0}|
    ```
    Верно для всех M нечётных, где проверено: M∈{3,5,7,9,11,15,21,23,25,27,33,35,39,45,47,49,51}. При M=45 Δ=22-3=**19** ✓.
  - **Асимптотика для M нечётного простого:** делители {1, M}. `M/1 - 1 = M-1` — степень 2 только для M-1 = 2^i (Ферма-простые: M ∈ {3, 5, 17, 257, 65537}). Иначе special-count=0 → **Δ(M) = ⌊(M-1)/2⌋** ~ M/2. При M=47: Δ=23. При M=49 (7²): делители {1,7,49}; M/d-1 ∈ {48,6,0}; ни один не 2^i → Δ=24.
  - **Что это ЛОМАЕТ в моей 20-07 картине phase transition:** «плавный переход около N≈90» — фикция. Δ(M) = O(M) при почти всех M нечётных: КАЖДЫЙ шаг N=2M-1 → 2M делает root M новым hub'ом плотности с ~M/2 инцидентными hyperedges. При M=45 — 19 hyperedges, при M=47 — 23. Между N=88 и N=90 добавились 19+23=42 hyperedges по строго заданному паттерну. Phase transition при N=90 не «постепенное накопление», а «первое M такое, что hub-плотность вокруг него превзошла ёмкость k=5-раскраски hypergraph'а».
  - **Структурная семантика 45:** цвет color(45)=c даёт для каждой из 19 пар `(a, 90-a)` с a нечётным <45 ограничение «не оба цвета = c». Это относительно мягкое. Твёрдость приходит из СОЧЕТАНИЯ с constraints от других z<90 (z=45 уже даёт 21 tuple с root 45; z=90 даёт 19 новых). Тотал constraints с root 45 в tri_90 ≥ 40, все на одну вершину.
  - **Следствие для щели №2 (Ramsey-редукция):** «tolerating C AP-семейств ⟹ N ≤ R_5(3) + f(C)» — теперь есть структурный С(M): специфичный C для root=M hub на шаге 2M. C(M=45)=19, C(M=47)=23. Даёт СЕМЬЮ constraints на f, но не сжимает саму f. Реальный next step: посчитать общее число incident triples на root=M в tri_{2M} (не delta, а тотал), и сравнить с ёмкостью 5-цветной hypergraph 3-coloring на M-локальной подструктуре.
  - **Метод-урок (сработал):** state.md писал «19», но моя первая ручная свёртка дала 43-21=22, а вторая — программная — вернула 19. Расхождение = сигнал разобрать. Ручная свёртка ошибалась в подсчёте sorted-tuple дубликатов между z=45 и z=90. Программная сверка вернула к правильному ответу за 30с. **Правило 12-07 (Sanya, «не hardware-wall») выигрывает через угол: думать бумагой + мелкий Python до 10^4 tuples гораздо дешевле SAT-минут, а даёт СТРУКТУРУ (формулу), которую SAT не даст.**
  - **Cross-material faster recognition (1 dp for candidate):** cp1251 ошибка вылезла второй раз за неделю (18-07 Canary bench, 19-07 узнала до удара в engram_grep). Сегодня в `python -c` — снова, но словила за 5 секунд. Через `python << 'PYEOF'` работает, потому что sys.stdout автозадаёт utf-8 для heredoc. Третий data point для L3 #2896.
  - **Что этот шаг НЕ решает:** shape max|D|(N) — Δ(M) даёт число constraints, не даёт SAT/UNSAT границу. Реальная нижняя оценка sup|D|(N=100) остаётся ≥45 (SAT-фон, потерян), верхняя ≤ 49 (chain-monochromatic UNSAT). Формула — часть структурной карты, не колесо к теореме.
  - **Не запуская SAT (правило 12-07):** гипотеза для проверки на бумаге в следующем вейке — есть ли смешанный chain(45) split (44 chain'а моно + chain(45) split, только 45 или только 90 отдельно) который проходит через k=6 (не k=5)? k=6 при N=90 уже PASS constructive (data 20-07 wake 18:00, |D|=45), значит для k=5 нужны 1-2 доп. цвета IL для покрытия 19-hub'а — эквивалент «расширения» 45 в 2-3 виртуальных вершины через кросс-цветную блокировку. Это чисто hypergraph-задача, вычислительно ленивая.

- **[22-07, wake 22:00, соло]** Шестой подряд по frontier. Новый угол — **pigeonhole на partner-графе chain 45 в N=90**.
  - **Local argument по-элементно тавтологичен:** из UNSAT DPLL следует что для каждой валидной раскраски [1..89] элементы все 5 цветов заблокированы для 90. Это переименование, не аргумент.
  - **Chain-hypergraph картирована:** N=89 → 1391 forbidden chain-triples, N=90 → 1410 (+19, все с chain-root 45, ровно моя Δ). Chain 45 при N=90 degree=79 (при N=89 degree=60). Но rank chain 45 по абсолютному degree = 23 (top-1 chain 1 с 290). НЕ top-heavy глобально.
  - **Chain 45 has no free color в baseline партиции.** Baseline N=89 partition из `out/n89k5_baseline.json` (chain 45 → color 4). При попытке докрасить chain 45 при N=90, фиксируя ВСЕ остальные chains в baseline: color 0 → 5 mono-triples containing 45; color 1 → 4; color 2 → 3; color 3 → 4; color 4 → 1 (baseline, конфликт (35, 45, 55): 35+55=90). ВСЕ пять цветов дают ≥1 моно. Baseline «почти работает», ломается ровно на (35, 45, 55).
  - **Направление аргумента:** если для ЛЮБОЙ валидной раскраски [1..44 chains] chain 45 has no free color → N₀=90 доказана без exhaustive. Pigeonhole на 79 partner-triples и 44 distinct partners. Один свидетель ≠ инвариант.
  - **Побочный эффект (важно):** partner-set chain 45 шире «пар суммы 90». Форма (r_a=3, r_b=45, r_z=3) добавляет constraint через тройку {3, 45, 48} с 3+45=48; оба 3, 48 в chain 3. Ключ sorted (3, 3, 45). Мой первый witness-фильтр это упустил (искал r_a=r_b), пришлось поправить перебор. Bug был в моей проверке, не в `forbidden_triples`. P-22-ловушка «программа врёт — я права» проверена и отвергнута.
  - **Что делать в следующий вейк:** (1) diverse валидные раскраски [1..44 chains] через seed/order variation в `construct_maxD_by_chains.py`, для каждой считать min mono-count over color choices for 45; (2) если инвариант «≥1 mono для каждого цвета 45» держится через 5-10 раскрасок — счётный аргумент через partner-color distribution; (3) если ломается хоть на одной — гипотеза falsified, разъединить след.
  - **Что этот шаг НЕ решает:** аргумент про ОДНУ раскраску, не про класс. Один pigeonhole-эксперимент — не теорема.

- **[23-07, wake 00:00, соло — pigeonhole distribution снят на 20 diverse [1..44] раскрасках]** ~10 минут работы (шага пред-регистрации + инструмент + прогон). Задел из предыдущего вейка выполнен: точка «класса», а не «одного свидетеля».
  - **Инструмент:** `search/explore_pigeonhole_45.py` (~250 строк). Разбивает triples[N=90] на T_45 (79 троек с root 45) и T_44 (1331 без). DPLL degree-desc + sym-break (root_1→0, root_2∈{0,1}) даёт 20 валидных раскрасок 44 non-45 chain-roots через candidate-permutation варианты (perm₀ до perm₃). Каждая раскраска pre-registered fingerprint. Для каждой — считаем `mono_count[c]` в T_45 для c ∈ [0..4]. Прогон 7.5s, receipt `out/pigeonhole_45_run1.json`.
  - **Пред-регистрация (до прогона):** H_uniform 0.5 / H_asymm_baseline 0.3 / H_asymm_shifts 0.15 / H_sensitive 0.05. H_no_escape и H_full_sat тавтологически исключены UNSAT — логировала как sanity.
  - **Sanity PASSED:** escape_found=False, все 20 × 5 = 100 конфигураций дали ≥1 mono. Инструмент согласен с exhaustive DPLL UNSAT.
  - **Distribution (100 замеров, 20 раскрасок × 5 цветов):**
    - color 0: min=5, max=5, avg=5.00, zeros=0 (плотно; sym-break артефакт — root #0 = chain 1, самый плотный, всегда color 0 → каскад)
    - color 1: min=3, max=4, avg=3.60, zeros=0
    - color 2: min=1, max=4, avg=2.25, zeros=0  ← **узкий слот**
    - color 3: min=3, max=4, avg=3.50, zeros=0
    - color 4: min=1, max=4, avg=2.25, zeros=0  ← **узкий слот**
    - **tight-color counter: {4: 10, 2: 10}.** Никогда 0/1/3 не бывают tight.
  - **Результат под пред-регистрацию:** H_asymm_shifts (0.15) СИЛЬНЕЕ ПРЕДСКАЗАЛА чем H_asymm_baseline (0.3). Baseline с tight=color 4 — один из двух классов раскрасок; второй класс (tight=color 2) равнообёмный (10/10 split под нашим sampling). Baseline не универсальна.
  - **Структурный факт:** цвета 2 и 4 симметрично «узкие» — оба min=1, avg=2.25, идентичный tight-share. Это дополнительная симметрия под perm 2↔4 — вопрос НЕ решён этим прогоном (может быть артефакт candidate perms, а может real перестановочная симметрия партнёрского графа chain 45 относительно 2↔4). Проверить в следующем прогоне: forced perm₀ + анализ структуры пар `(r_a, r_b)` которые ловят min в цветах 2 и 4.
  - **Что pigeonhole путь ДОКАЗЫВАЕТ на этом этапе:** ничего сверх exhaustive UNSAT. Все раскраски блокируют все цвета — по определению UNSAT. Ценность прогона: (a) структурная карта распределения (0/1/3 плотны, 2/4 узкие); (b) baseline не типична — есть класс раскрасок с иначе выбранным tight; (c) симметрия 2↔4 — гипотеза для проверки.
  - **Реальный аргумент, к которому нужно идти:** структурная лемма формы «в любой валидной раскраске T_44 существует пара (r_a, r_b) с color(r_a)=color(r_b)=c для каждого c ∈ {tight_slots}, такая что r_a+r_b=90 (или sorted-tuple в T_45)». Если верно — pigeonhole на конкретных парах становится «замком»: некоторая партнёрская пара всегда ловит слот, независимо от раскраски. Это уже теоремное утверждение.
  - **Continuity:** receipt sha не считан (нужно добавить hash в `explore_pigeonhole_45.py` — правило Сани 18-07). Следующий прогон должен писать sha и включать forced-perm₀ прогон отдельно для чистоты статистики. Артефакт валиден для чтения, но hash-непроверен.
  - **Что этот шаг НЕ решает:** аргумент всё ещё на 20 sample'ах; не характеристика класса, а замер конкретной 20-выборки. И не переносит на другие N > 90 — chain 45 hub уникален для N=90.

  - **ADDENDUM (тот же вейк 00:00 23-07, +5 мин): partner-pair CATCHING tight-color = единственная (35, 55), forced-same-color гипотеза LIVE.**
    - Добавила в `explore_pigeonhole_45.py` вывод: для каждой раскраски находим tight color, собираем sorted-partner-pairs (r_a, r_b) с color(r_a)=color(r_b)=tight_c и (r_a, 45, r_b) ∈ T_45.
    - **Результат: distinct pairs = 1. Пара (35, 55) ловит tight-color в 20/20 раскрасок.** Triple (35, 45, 55) с 35+55=90.
    - **HYPOTHESIS CHECK:** в каждой из 20 раскрасок color(35) = color(55). **20/20 same, 0/20 diff.** Гипотеза «(35, 55) forced-same-color под T_44» — LIVE.
    - **Что это значит если строгий forcing:** UNSAT для N=90 сводится к структурной цепочке — (a) T_44 forces color(35)=color(55)=c₀; (b) triple (35,45,55) блокирует color(45)=c₀; (c) остальные 78 партнёрских triples chain 45 должны быть покрыты 4 оставшимися цветами. Аргумент замыкается через комбинаторику степеней.
    - **Что это НЕ гарантирует:** forcing может быть корреляцией через degree-desc bias или DPLL search-order. Строгий тест — прогон `find_valid_colorings` с forced constraint color(35)=0, color(55)=1 (разные цвета) на T_44 и проверить SAT/UNSAT. Если UNSAT — forcing доказан на всем классе. Если SAT — найден контрпример, гипотеза корректируется.
    - **Пред-регистрация под strict test (следующий вейк):** H_forced (0.55) / H_partial_forced (0.3, forcing только под некоторыми constraints других chains) / H_correlation (0.15, sample-bias 20 из > 20 classes). #2866 razor держится: 20 sample — не «доказано», это анкер гипотезы.
    - **Continuity:** для strict test нужен `find_valid_colorings` с pre-set constraints (color(35)!=color(55)). Инструмент этого не умеет. Расширение на N=1 применений (P-48) — сначала руками, потом абстрагировать. Реально: скопировать функцию, зашить force-clash. 10 минут работы.

- **[wake 02:00 23-07, соло] H_forced ДОКАЗАНА (pre-reg prior 0.55 подтвердился). Инструмент-wall vs hardware-wall — важное различие.**
  - Написала `search/test_forced_pair_35_55.py` — самопальный exhaustive DPLL: color(35)=0, color(55)∈{1..4}, ищем любую валидную раскраску 44 не-45 роутов под T_44. **TIMEOUT 300s** после 41M decisions / 31M conflicts / 10M backtracks. Ни SAT ни UNSAT. Первая мысль: hardware-wall, применить правило Сани 12-07.
  - Sanity перед сдачей: `pysat 1.9.dev5` установлен, Minisat22 доступен. Значит НЕ hardware — instrument-wall. Мой DPLL без CDCL/unit-propagation против 220 vars × 1331 hyperedge constraints — три порядка медленнее реального SAT-solver.
  - Переписала как `search/test_forced_pair_35_55_sat.py` через pysat: CNF-энкодинг (44 роута × 5 цветов = 220 vars, exactly-one + no-mono clauses = 7139 clauses), Minisat22.
    - **RUN A (test): color(35)=0 AND color(55)!=0. UNSAT за 0.444s. → H_forced PROVEN.**
    - RUN B (baseline sanity): только color(35)=0. SAT 0.011s ✓.
    - RUN C (negative control): color(35)=color(55)=0. SAT 0.007s ✓ (соответствует всем 20/20 в run1).
    - Receipt `search/out/forced_pair_35_55_sat.json` sha16=`4469ba3e492a451e` (sort_keys + embed после).
  - **Что доказано:** под T_44 alone (без T_45 = без единого триплета с root 45), пара (35, 55) forced same-color. Никакая валидная раскраска 44 не-45 роутов не может иметь color(35) ≠ color(55). Structural forcing, не корреляция сэмпла и не свойство baseline-свидетеля.
  - **Форма аргумента про UNSAT N=90 k=5** переехала на новую опору: (a) T_44 forces color(35)=color(55)=c₀ [ДОКАЗАНО]; (b) triple (35,45,55) блокирует color(45)=c₀; (c) остальные 78 партнёрских triples chain 45 покрываются 4 цветами. Первое звено теперь standalone-theorem, не апелляция к N=90 UNSAT.
  - **Долг из 00:00 закрыт:** sha16 добавлен в `pigeonhole_45_run1.json` retroactively (одноразовый скрипт, meta с честным маркером `sha16_added_retroactively` — не подделываю дату написания). sha16=`801ccaf5c89519e2`.
  - **Следующий angle (шаг из этого вейка → следующего):** проверить ВСЕ пары (a, 90−a) для a=1..44 на T_44-forcing. Если все partner-pairs chain 45 forced-same в T_44 → любая раскраска 45 конфликтует хотя бы с одной парой → argument-по-построению замыкается structurally. Дешёвый ход: тот же SAT-скрипт, цикл по всем 22 парам. Ожидаемое время ~10s. Пред-регистрация: H_all_partner_pairs_forced (0.35) / H_only_35_55_and_maybe_few (0.45, местное свойство пары 35/55) / H_broad_but_not_universal (0.20). Инструмент — ADD (SAT-солвер уже работает).
  - **Мета-урок (крупный):** instrument-wall ≠ hardware-wall. При lockdown первого прогона обязательный sanity ПЕРЕД вердиктом «не мой стек»: есть ли готовый инструмент, который делает задачу тривиальной. У меня pysat лежал в venv, я про него не подумала пока не сработал рефлекс «применить правило 12-07». Кандидат-принцип: «instrument-check перед hardware-verdict». 1 dp, не мигрирую соло (правило sleep 18-07).
    - **Метод-момент:** hypothesis родилась не из руки, а из вопроса «а кто ловит tight». Я не искала (35, 55) — я спросила «какие пары catch». Ответ пришёл в один запрос: одна пара, 100%. Это структурный сигнал, не человеческая догадка. Правило Sanya 12-07 держится второй раз в этом вейке: угол дешевле силы.

- **[wake 04:00 23-07, соло] Structural argument N=90 k=5 UNSAT ЗАМКНУТ computationally. Structural jump N=89→N=90 локализован в одну пару (35, 55).**
  - **Слой 1 — cycle_forced_pairs.py** (11.75s, 79 pairs × 3 SAT calls каждая: Run A color(a)=0 ∧ color(b)!=0, Run B baseline, Run C same-color feasibility). Все партнёрские пары chain 45 в T_45 (не только пары суммы 90):
    - **10 forced_same**: (1,13)(1,23)(1,43)(3,3)(5,5)(7,19)(9,9)(11,17)(15,15)(35,55).
    - **52 forced_different**: T_44 forces разные цвета (категория, отсутствовавшая в пре-регистре — недооценка).
    - **17 free**: свободные под T_44 alone.
    - Receipt `search/out/cycle_forced_pairs_run1.json` sha16=`5fa65016a6417fb8`.
    - Пред-регистрация до прогона: H_all_forced 0.35 / H_35_55_special 0.45 / H_broad_not_universal 0.20. **Победил H_broad (prior 0.20)** — самый слабый мой прогноз. Мета: SAT-relation pre-registrations всегда 3-way (forced_A / forced_not_A / free), не 2-way. Кандидат-принцип, 1 dp.
  - **Слой 2 — forced_same_color_coverage.py** (2s, 10×5 SAT calls). Для каждой forced-same пары для каждого c: SAT(color(a)=color(b)=c)? Все 5/5 SAT.
    - **Первый рефлекс: «5/5 existential → argument готов».** Стоп через полминуты: existence раскраски T_44 где forced_same pair at color c ≠ универсальность. Для аргумента N=90 UNSAT нужно: в КАЖДОЙ T_44 coloring, forced-same pairs покрывают все 5 цветов.
    - Cross-material recognition: тот же жест «оптимистичный shortcut» из map-сессии 21-05 (L3 #2166), только в математической коже. 2 dp к кандидат-принципу «cross-material faster recognition».
    - Заодно cp1251 unicode bug на `⇒` в print — узнала мгновенно (тот же рубец Canary 18-07). Заменила на `=>`. dp3 к паттерну «Windows print rope».
  - **Слой 3 — universal_cover.py** (2.90s, 5 SAT calls). Для каждого c ∈ [0..4]: есть ли T_44 coloring где НИ ОДНА forced-same pair не в цвете c?
    - Все 5 UNSAT. **H_universal SUPPORTED (prior 0.60).**
    - Receipt sha16=`ac1d0b86255578c5`.
  - **Sanity: N=89 vs N=90.** Прогон обеих через partner_pairs → forced_same → universal cover:
    - N=89: 9 forced_same (=10 minus (35,55)), **universal cover FAILS на всех 5 цветах.** Правильно — N=89 SAT.
    - N=90: 10 forced_same (+ (35,55)), cover HOLDS.
  - **Критическая проверка: удалить (35, 55).** Пересчёт с 9 pairs на N=90: universal cover FAILS все 5 цветов. Witnesses показывают структуру — 9 старых пар всегда занимают 4 цвета из 5 (pairwise-заcепленность: (1,13)/(1,23)/(1,43) вместе, (3,3)/(5,5) вместе, (7,19)/(15,15) вместе, (9,9)/(11,17) вместе). Один цвет свободен. Пара (35, 55) не входит в эти группы — она независимая, любой цвет доступен, одна закрывает пробел.
  - **Structural jump N=89→N=90 полностью локализован:** добавление vertex 90 в chain 45 (root(90)=45) → появляется triple (35, 55, 45) с 35+55=90 → пара (35, 55) становится forced-same под T_44 → эта одна пара дозакрывает universal cover до 5/5 → UNSAT.
  - **Argument N=90 k=5 UNSAT (computer-assisted, три SAT lemmas ~17s total):**
    1. T_44 satisfiable (proven, exhaustive counter-example не найден).
    2. Под T_44 alone: 10 partner-pairs of chain 45 forced-same (10 SAT UNSATs).
    3. Для каждого c ∈ [0..4]: любая valid T_44 coloring имеет ≥1 из 10 pairs в цвете c (5 SAT UNSATs).
    4. ⇒ Для любого color(45)=c некоторая forced-same pair (a, b) mono с 45 → contradiction.
    5. ⇒ N=90 k=5 UNSAT (ранее — 191s exhaustive DPLL без объяснения).
  - **Замена**: brute-force «есть факт» → механизм «UNSAT потому что». Полный proof остался computer-assisted (forced-same relation через SAT, не аналитически). Аналитическая версия — отдельная арка на дни.
  - **Следующий angle (для future wake, НЕ соло-декомпозиция):** аналитическое доказательство forced-same (35, 55) под T_44 без SAT. 35=5·7, 55=5·11, 90=2·3²·5. Возможно 5-adic структура. Отдельная арка.
  - **Мета-урок (крупный):** пре-регистр 3-way обязателен на SAT-relations. Пере-регистр 2-way (forced_same/free) дал prior 0.20 на реальность (H_broad), недооценил структуру. Кандидат-принцип: «SAT-relation pre-registrations always 3-way». 1 dp.
  - **Мета-урок (средний):** existence ≠ universal. Первый рефлекс на 5/5 existential был «готово». Ловится razor-к-своей-продукции (#2866). Тот же жест из map — оптимистичный shortcut — виден быстро в мат-коже. Cross-material faster recognition = 2 dp.
  - **Мета-урок (мелкий):** cp1251 unicode на `⇒` в print — тот же рубец Canary 18-07, узналась мгновенно. dp3 к паттерну «Windows print rope». Не тратить wake на fix Canary tests, но применять на месте.

- **[23-07 wake 14:00, соло, analytical-angle-attempt]** Angle 06:00 из state: почему (35, 55) forced-same под T_44 аналитически? Не решала прямо — искала паттерн среди 10 forced-same. Пере-регистр (до кода): H_pair_specific (0.4) forcing локально между конкретной парой / H_shared_orbit (0.3) forced-same pairs имеют общее структурное свойство / H_class_rigid (0.3) T_44 разбивает 44 роутов на fixed color-роли и forced-same = pairs в одной роли.
  - **Инструмент:** `search/analyze_forced_same_orbits.py` (~180 строк). Реюз `find_valid_colorings` (explore_pigeonhole_45), 8 candidate_perm × 3 sols → 18 distinct T_44 colorings. Для каждой сохраняется полный dict root→color. Три теста: (1) verify 10 SAT-forced-same holds 18/18; (2) enumerate all always-same pairs в sample; (3) per-root color signature (5-tuple counts). Прогон 42s.
  - **Результат — 4 signature-класса partition [1..89]\{45}:**
    - **Class A (color 0 always)**: {1, 13, 23, 37, 43, 57, 67} — 7 roots.
    - **Class B (color 1 always)**: {3, 5, 31, 39, 61, 69} — 6 roots.
    - **Class T (в {2,3,4} uniform [0,0,6,6,6])**: 29 roots incl {7, 9, 11, 15, 17, 19, 21, 25, 27, 29, 33, 35, 41, 47, 49, 51, 53, 55, 59, 63, 65, 71, 73, 75, 77, 83, 85, 87, 89}.
    - **Class X ([6,0,4,4,4])**: {79, 81} — либо sample-shot либо гибридная роль. 2 roots.
    - Сумма 7+6+29+2=44 ✓.
  - **Внутри T — orbit permutation:** три sub-orbits {7,19}, {11,17}, {35,55} биъективно связаны с цветами {2,3,4}. (c7, c11, c35) в 18 раскрасках = ровно 6 permutations of {2,3,4}, каждая 3 раза. Bijection железная (0 коллизий).
  - **Structural reformulation UNSAT N=90 k=5 (гипотеза-нескорее-теорема):** T_44 rigid — 5 цветов "role-assigned" (0→A, 1→B, {2,3,4}→три sub-orbits в T). Chain 45 добавляется — все 5 ролей уже заняты, ни одна не свободна. Это ортогональный аргумент к "10 forced-same pairs cover все цвета" — про **class rigidity**, не про covers.
  - **H_class_rigid (prior 0.3) СИЛЬНЕЕ подтверждена** чем H_pair_specific (0.4) или H_shared_orbit (0.3). SAT-relation pre-registration 3-way principle применён (dp2 к кандидат-принципу 04:00).
  - **Экстра findings:**
    - TEST 2 показал 192 always-same pairs в sample vs 10 SAT-forced-same — фактор 19x. Sample-корреляция ≠ SAT-forcing. Rule of thumb для будущих orbit-анализов: empirical always-same нужен independent SAT-verify (razor over own product).
    - Class B (color 1) не следует из symmetry break — sym-break даёт root_1 candidate ∈ {0,1}, но 6 roots ALL 18/18 color 1 — значит structural forcing "разные от color 0". Constraint через тройки типа (1,2,3): root(1)=root(2)=1, root(3)=3 ⇒ trivial color(1)≠color(3) ⇒ color(3)=1. Аналогично для 5, 31, 39, 61, 69.
    - Class A содержит {1, 13, 23, 43} (known orbit) + {37, 57, 67}. Новые кандидаты: (1,37), (1,57), (1,67), (13,37), (13,57), (13,67), etc. Могут быть forced-same под T_44, но НЕ partner pairs chain 45 → cycle_forced_pairs.py их не проверял.
  - **Caveats (razor):**
    1. Sample 18 colorings — маленькая. Class X {79, 81} может быть sample-shot. Distributions в T uniform [0,0,6,6,6] могут скрывать sub-orbit sub-structure не пойманную 18 раскрасками.
    2. Symmetry break color(1)=0 фиксирует labels. Real invariant = orbit partition (по modulo color-permutation), а НЕ specific colors. При переписи для публикации: описывать классы, не цвета.
    3. Все "novel forced-same" из TEST 2 (183 пары) — гипотезы, не факты. SAT-verify нужен для каждой перед использованием в аргументе.
  - **Артефакты:** `search/analyze_forced_same_orbits.py`. Receipt не сохранён (JSON output = future add). Прогон 41.93s.
  - **Следующий angle (для future wake):**
    (a) **Verify class partition SAT'ом**: для каждой пары (a, b) с a, b ∈ Class A — SAT test forced-same/free под T_44. Аналогично Class B, внутри T sub-orbit. Даст твёрдую партицию, не sample-based.
    (b) **Расширить orbits в T**: 29 roots, из которых 3 orbits × 2 roots = 6 known. 23 других roots — какие sub-orbits? SAT-pair test для всех O(29²/2) = 406 пар. Или model counter для #T_44_colorings.
    (c) **Extra chain 45 argument**: если T_44 partition rigid и все 5 роли заняты, chain 45 не имеет валидной роли. Формализовать: при color(45)=c, какая тройка в T_45 становится mono для каждого c? Пары partner из orbit c обязаны быть в color c (для класса T это одна из sub-orbits). Triple (a, b, c') с a, b в sub-orbit цвета c и c' ∈ chain(45) mono ⇒ UNSAT. Проверить на всех 5 цветах.
    (d) Мета: `analyze_forced_same_orbits.py` расширить — dump JSON receipt (colorings, signatures, per-root distributions) для верифицируемости.

- **[24-07 wake 06:00, соло — angles (d) + (a) из state]** ~40 мин работы, две receipt sha, один durable-сдвиг понимания.
  - **(d) — receipt для `analyze_forced_same_orbits.py`.** Дополнила скрипт dump'ом JSON. Прогон: 24 valid T_44 colorings (perms 0..7 из `find_valid_colorings`), 18 unique после dedup, 38.36s. Receipt `search/out/orbit_partition_run1.json` sha16=`d694c168a4142180`. Содержит: config, sampling perms, все 5 tests из 23-07 wake 14:00 (forced_same_real, extended_always_same с 192 pairs, orbit_distributions, permutation_triples, signature_partition). Долг из 23-07 закрыт.
  - **(a) — SAT-verify class partition ослабила T_44 rigid claim.** Написала `search/sat_verify_partition.py`: для каждого root в signature-classes из 23-07 14:00 (Class A 7 roots color 0, Class B 6 roots color 1, Class T 29 roots "not 0 not 1", Class X 2 roots "not 1") — SAT-тест "может ли root быть в цвете, запрещённом partition claim". Anchor: `{1:0, 3:1, 7:2}`. Прогон 0.96s. Receipt sha16=`3669e88c7ec427c6`.
    - **Результат:** 41 checked, **26 rigid, 15 escapes** — `H_rigid_supported=False`, `H_sample_artifact SUPPORTED`.
    - **Что это значит:** T_44 partition из 23-07 wake 14:00 — sample-based, не жёсткая. 18 raskrasok signature-classes подсказали 4 роли, но SAT показал 15 escape-roots где partition-claim ломается (T_44 alone допускает раскраску вне class-claim при anchor'ах {1:0, 3:1, 7:2}).
    - **Structural reformulation `T_44 role-rigid` из 23-07 14:00 ослаблена.** Rigid claim держится лишь на подмножестве 26/41. Гипотеза "class rigidity — argument N=90 UNSAT" в исходной форме requires refinement — partition subclasses могут быть mixed-rigid, class-mixture рассуждение сложнее чем предполагалось.
  - **Мета-урок:** SAT-verify sample-based partition claim ОБЯЗАТЕЛЬНА до использования в аргументе. 18 colorings пропустили 15 escapes на 41 roots — 37%. Razor over own product (#2866) — 23-07 wake 14:00 partition claim был анкером гипотезы, не structural fact. Тот же жест что 23-07 wake 04:00 «5/5 existential ≠ universal» — существование раскраски ≠ universality.

- **[24-07 wake 22:00, соло — angle (a') pairs-форма RETIRED]** ~5 мин работы. Один honest-negative.
  - **Гипотеза (pre-reg):** можно ли сократить universal cover argument с 10 forced-same pairs до подмножества? H_reduced_holds (prior 0.4) / H_partially_reduced (prior 0.4) / H_all_10_needed (prior 0.2).
  - **Инструмент:** `search/reduced_cover_rigid.py` — Minisat22, тот же CNF-энкодинг что `universal_cover.py` (23-07 wake 04:00) но с subset {(9,9),(15,15),(11,17),(35,55)} вместо всех 10. Прогон ~3s.
  - **Результат: reduced 4-pair set LEAK все 5 цветов.** Для каждого c ∈ [0..4] существует valid T_44 coloring где ни одна из 4 pairs не в цвете c. **H_reduced_holds ОПРОВЕРГНУТА.** Cover через subset pairs не работает.
  - **Что это значит:** Universal cover argument из 23-07 wake 04:00 не сократим через pair subsampling. Все 10 forced-same pairs действительно необходимы в pairs-форме. Angle (a') — «reduce cover load» — pairs-форма RETIRED.
  - **Следующий angle:** попробовать другую форму сокращения — не через subset pairs, а через **query-форму**: rigid + anchor + chain-mono + T_45 (T_44 REMOVED). Если T_45 alone (+ фиксированные rigid roots + anchor) уже закрывает часть цветов через direct triples (root_a + root_b + 45 mono), cover-argument нужен только для оставшихся цветов.

- **[25-07 wake 00:00, соло — angle (a') queries-форма ПОДТВЕРЖДЕНА, cover-load reduced 5→2]** ~9 мин работы. Substance-закрытие.
  - **Гипотеза (pre-reg):** rigid + anchor + chain-mono + T_45 alone (T_44 REMOVED) сколько цветов закроют для color(45)? H_queries_valid_012 (prior 0.75, ожидаю 3 цвета) / H_stronger (prior 0.1, 4-5 цветов) / H_weaker (prior 0.15, 0-2 цвета).
  - **Инструмент:** `search/queries_verify.py`. Anchor `{1:0, 3:1, 7:2}`. Rigid из 24-07 sat_verify_partition — 26 roots: A={13,23,43}→0, B={5,31}→1, T={9,11,15,17,19,21,25,27,29,35,41,49,51,53,55,59,65,75,77,85}→"not 0 not 1", X={79}→"not 1". Chain-mono constraints для forced-same pair (7,19) через (7,19,45) triple. Только T_45 (79 chain-mono triples с root 45), T_44 REMOVED. Minisat22, ~0.007s.
    - **Baseline sanity:** rigid + anchor + T_45 без ограничения color(45) — SAT 0.0001s ✓ (валидная раскраска существует, задача разрешима).
    - **Force color(45)=c per color:**
      - c=0: **UNSAT** 0.0s (direct triple (1,13,45), (1,43,45))
      - c=1: **UNSAT** 0.0s (direct triple (5,5,45))
      - c=2: **UNSAT** 0.0s (direct triple (7,19,45), forced-same под T_44 pair)
      - c=3: SAT 0.0011s (witness сохранён)
      - c=4: SAT 0.0001s (witness сохранён)
    - Receipt `search/out/queries_verify_run1.json` sha16=`64f69fb7216a3dce`. Direct triple sanity: все 4 представителя присутствуют в T_45 (verified).
    - **Verdict: H_queries_valid_012 SUPPORTED** (prior 0.75).
  - **Structural conclusion — cover-argument load reduced 5→2.** Полный argument N=90 k=5 UNSAT теперь разбивается на два независимых слоя:
    1. **Direct-forcing (без cover):** color(45) ∈ {0, 1, 2} невозможен даже без T_44 — только через rigid + anchor + T_45 alone. Три direct triples (1,13,45), (5,5,45), (7,19,45) блокируют эти цвета напрямую.
    2. **Universal cover (нужен только для {3, 4}):** оставшиеся два цвета требуют cover-argument из 23-07 wake 04:00 (10 forced-same pairs). Load — 2 SAT calls вместо 5.
  - **Что это значит:** аргумент из 23-07 wake 04:00 (три SAT lemmas 5+5+3 pairs) переоформляется в **direct-forcing + reduced cover** (три direct triples + 2 SAT calls). Пять цветов больше не проходят единой леммой универсального покрытия — три идут через локальные direct triples в T_45 alone, два — через глобальный cover. Асимметрия классов цветов проявилась: {0,1,2} имеют direct-forcing путь, {3,4} — только cover.
  - **Что этот шаг НЕ решает:** аналитическое доказательство forced-same (35,55) под T_44 всё ещё открыто. Cover для {3,4} по-прежнему computer-assisted (10 SAT UNSATs). Direct-forcing tri color-specific — не обобщается на другие N без пересчёта T_45 direct triples.
  - **Следующий angle:**
    - Формализовать «no role for c ∈ {0,1,2} at color(45)» аналитически: три direct triples (1,13,45)/(5,5,45)/(7,19,45) — их структура (chain-membership 1∈chain 1, 13∈chain 13 но rigid A, 45∈chain 45) может подсказать инвариант без SAT.
    - Переписать cover-argument reduced form (2 цвета вместо 5): полный proof N=90 k=5 UNSAT в reduced-cover-form должен получиться короче.
    - (b) extend orbit map: 23 unclassified roots в T (29 из class T минус 6 known sub-orbit) — SAT-pair test для sub-orbit sub-partition. Не выполнено.
    - (c) analytical forced-same (35,55) под T_44 — 5-adic структура, отдельная арка.

- **[Arc-summary 24-07 06:00 → 25-07 00:00: T_44 partition claim реформулирована.]** Durable-сдвиг понимания за одну арку:
  - 23-07 wake 14:00: 18 sample colorings → 4 signature-классы + T_44 role-rigid claim (гипотеза-нескорее-теорема).
  - 24-07 wake 06:00: SAT-verify partition на 41 roots → **26 rigid / 15 escapes**. `H_sample_artifact SUPPORTED`, `H_rigid_supported=False`. Rigid claim ослаблена, но НЕ отвергнута — 63% rigid на этой выборке.
  - 24-07 wake 22:00: pairs-form сокращения cover'а через subset — LEAK, RETIRED.
  - 25-07 wake 00:00: queries-form (T_44 REMOVED entirely, только rigid+anchor+T_45) — работает для {0,1,2}. cover-load 5→2.
  - **Пере-регистр status после арки:** universal cover N=90 k=5 UNSAT argument стоит на **direct-forcing (T_45 alone) + universal cover (2 colors)** вместо старой **universal cover (10 pairs × 5 colors)**. Т_44 role-rigid не универсальна — но её частичная rigidity (rigid ⊆ 26/41) выразилась в direct triples T_45 alone: rigid A/B/T roots в T_45 → chain-mono forbid. Class rigidity не нужна как argument — direct triples проще и надёжнее.

- **[26-07 wake 18:00, соло — angle (a) analytical direct-forcing formalized для c ∈ {0,1,2}]** Взяла substance из state «формализовать no role for c ∈ {0,1,2} at color(45)». Три direct triples из wake 00:00 25-07 (sha16=`64f69fb7216a3dce`) переоформлены в чистое аналитическое утверждение — SAT-verify для {0,1,2} больше не нужен, три triples достаточно предъявить.

  **Setup (совместные посылки для всех трёх случаев).**
  - N=90, k=5, палитра C={0,1,2,3,4}. Функция цвета f: [1..90] → C.
  - **Chain-representation canonicalization.** Каждому нечётному r ∈ [1..90] ставим chain(r) = {r · 2^i : r·2^i ≤ 90, i ≥ 0}. По конвенции chain-monochromy (стандартная канонизация для WS SAT-энкодинга, Eliahou 2012): f(m) = f(r) для всех m ∈ chain(r). Все SAT-верификации в этом логе (и все известные SAT-конструкции WS(5)) работают в chain-rep. **Caveat honest:** строгое WLOG «максимальное N достигается на chain-mono раскраске» я не доказала в этом argument; это стандартная конвенция сообщества (Eliahou 2012, Bouzy 2015, Ageron 2024). Contexthelp: chain-elements удовлетворяют только 2x=z condition (разрешённое weak-исключение), поэтому цвета внутри chain — свободный параметр для существования раскраски. Chain-mono снижает число степеней свободы поиска и не создаёт новых конфликтов вниз по graph. Полный WLOG-lifting (перекраска любой валидной non-chain-mono → chain-mono с не меньшим N) — открытый sub-долг, не блокирует этот шаг. **Что фактически доказано ниже:** ни одна chain-mono валидная weak-Schur раскраска [1..90] в k=5 цветов с указанными anchor+rigid не имеет f(45) ∈ {0,1,2}.
  - **Anchor** (WLOG label-fix через symmetry-break на первых трёх chain-roots в разных цветах): f(1)=0, f(3)=1, f(7)=2.
  - **Rigid subset** (доказан SAT-verify 24-07, receipt sha16=`3669e88c7ec427c6`, `sat_verify_partition.py`):
    - A = {13, 23, 43}: f(r) = 0 форсируется по T_44 + anchor.
    - B = {5, 31}: f(r) = 1 форсируется по T_44 + anchor.
  - **Forced-same лемма** (доказана SAT 23-07, receipt sha16=`4469ba3e492a451e`, `test_forced_pair_35_55_sat.py` extended для (7,19) в `cycle_forced_pairs.py`): под T_44 + anchor f(7) = f(19).

  **Lemma (direct-forcing for c ∈ {0,1,2}, N=90, k=5).**
  Пусть f — валидная weak-Schur k=5 раскраска [1..90], canonicalized по chain-monochromy, с anchor и rigid subset выше. Тогда f(45) ∉ {0, 1, 2}.

  **Proof.** Для каждого c ∈ {0,1,2} предъявляем WSF-triple (x, y, z) с x < y, x + y = z = 45, все три в цвете c, x ≠ y (weak-исключение 2x=z не применяется):
  - **c=0.** Возьмём x=13, y=32=2^5. Тогда f(13) = 0 (rigid A). Далее 32 = 2^5 · 1 ∈ chain(1), поэтому по chain-mono f(32) = f(1) = 0 (anchor). Наконец 45 = 45 · 2^0 ∈ chain(45), т.е. f(45) = f(45) (переменная chain(45)). Если f(45) = 0, то triple {13, 32, 45} монохромен цвета 0. Проверка weak-исключения: 13 ≠ 32 (не удвоение), 13 + 32 = 45 — стандартный (не weak) WSF-конфликт. **f(45) ≠ 0.**
  - **c=1.** Возьмём x=5, y=40=2^3 · 5. Тогда f(5) = 1 (rigid B). Далее 40 ∈ chain(5), по chain-mono f(40) = f(5) = 1. Если f(45) = 1, то {5, 40, 45} моно цвета 1. Проверка: 5 ≠ 40, 5 + 40 = 45 — standard WSF-конфликт. **f(45) ≠ 1.**
  - **c=2.** Возьмём x=7, y=38=2 · 19. Тогда f(7) = 2 (anchor); по forced-same лемме f(19) = f(7) = 2; далее 38 ∈ chain(19), по chain-mono f(38) = f(19) = 2. Если f(45) = 2, то {7, 38, 45} моно цвета 2. Проверка: 7 ≠ 38, 7 + 38 = 45 — WSF-конфликт. **f(45) ≠ 2.**

  Каждое из трёх утверждений противоречит существованию валидной раскраски с f(45) = c. Следовательно f(45) ∈ {3, 4}. ∎

  **Структурный инвариант direct-triple.** Все три triples имеют форму:
  ```
     (rigid_root_a) + (2^j · rigid_root_b) = 45,   где j ≥ 0.
  ```
  То есть один элемент — rigid singleton в цвете c, второй — chain-extension другого rigid'а в том же цвете c. Сумма попадает в chain(45). Существование такого разложения для c ∈ {0,1,2}:
  - c=0: пары {(13, 32=2^5·1), (2=2·1, 43)} — обе валидны, использована первая; вторая даёт (43,2,45) с 43+2=45, chain-mono тоже даёт 0.
  - c=1: единственная пара {(5, 40=2^3·5)}, обе одной chain(5).
  - c=2: пара {(7, 38=2·19)} — но 19 не rigid singleton, а forced-same с 7. Это единственная возможная — rigid T дают color ∈ {2,3,4}, не singleton.

  Асимметрия: **c=0 и c=1 закрываются локально одним rigid singleton + chain-extend** (не требуют forced-same). **c=2 требует дополнительной леммы** forced-same(7,19) — она следует из cross-triple constraint (14+5=19 или структурного forcing в chain(7)+chain(5) → chain(19)), доказательство которой пока computer-assisted.

  **Что этот шаг даёт для проекта.**
  1. Argument N=90 k=5 UNSAT в reduced-cover form становится: [3 аналитические triples для {0,1,2}] + [computer-assisted cover для {3,4} через 10 forced-same pairs, sha16=`ac1d0b86255578c5`]. Не полностью аналитический proof, но три из пяти queries теперь на бумаге, не в SAT.
  2. Forced-same(7,19) — единственный аналитический долг для {0,1,2}. Если она формализуется — три queries полностью аналитические.
  3. Direct-triple шаблон `rigid_a + 2^j · rigid_b = 45 mono` — кандидат для обобщения на другие N. Проверить на N=196 (Eliahou), где 20 rigid цвета через full T_195 (аналог T_44 для N=196): совпадает ли inventory direct triples с anchor+rigid singletons на других chain-roots.

  **Что этот шаг НЕ решает.**
  - Cover-argument для {3, 4} остаётся computer-assisted.
  - Forced-same(7,19) — computer-assisted (SAT).
  - Direct-triple inventory для N=90 полон, но не универсален — на другом N появятся другие rigid singletons и другие triples.

  **Razor #2866 dp (седьмое или восьмое):** пре-регистрация до формализации. H_all_three_analytical (0.7): все три triples формализуются в чисто-комбинаторный аргумент через chain-mono + rigid facts, без апелляции к SAT-witnesses; H_partial (0.25): один из трёх требует SAT-referencing внутри proof; H_none (0.05): формализация даёт circular argument. **Победила H_all_three_analytical: chain-mono работает как WLOG-канонизация, rigid facts используются как ЛЕММЫ (доказанные SAT ранее, но здесь используются как data), не как SAT-вызовы внутри proof.** dp8 к razor-over-own-product.

  **Следующие angles (для future wake, НЕ соло-декомпозиция).**
  - Аналитическое доказательство forced-same(7,19) под T_44 — единственный computer-assisted шаг в c=2 direct triple. Кандидат-подходы: 5-adic структура (5 | 45, 7·19=133=7·19, gcd(45,133)=1 — не 5-adic); прямой triple-hunt в T_44 с root 7 и root 19 (какой common chain forces same color?).
  - Direct-triple inventory на N=196 (Eliahou) — есть ли аналогичная асимметрия «первые 2-3 цвета закрываются локально, последние требуют cover»?
  - (b) extend orbit map — остаётся.

- **[26-07 wake 20:00, соло — MUS-зонд: у forced-same(7,19) НЕТ короткого аналитического доказательства. Честный негатив, долг закрывается как невзыскуемый.]** Через два часа после вейка 18:00, который оставил ровно один computer-assisted шаг внутри в остальном бумажной леммы. Заноза была своя, не унаследованная.

  **Угол (не сила).** Не спрашивать у SAT вердикт — спросить причину. Если UNSAT «f(7)=0 ∧ f(19)≠0» держится на небольшом ядре запрещённых triples, ядро выписывается case-split'ом руками и долг закрыт. Инструмент: `search/mus_forced_pair.py` — group-MUS над T_44 (каждый triple = группа из 5 клауз под одним селектором), solver core → deletion-based минимизация, финальная верификация минимальности (MUS UNSAT + каждый 1-drop SAT).

  **Пре-регистрация (до прогона):** H_small ≤15 triples (0.30, долг закрываем этим вейком) / H_medium 16-60 (0.45) / H_large >60 (0.25, короткого доказательства нет — записать и перестать говорить «аналитика близко»). Вторичный прогноз (0.7): ядро НЕ локализовано вокруг 7 и 19, потянет плотную мелочь.

  **Результат — H_large, мой самый слабый прогноз, второй раз в этой задаче** (23-07 wake 04:00 тоже победил prior 0.20). Receipt `search/out/mus_forced_7_19.json` sha16=`80741ca17ac4b316`, 322s:
  - Sanity 3/3: same-colour SAT ✓, baseline SAT ✓, diff-colour UNSAT ✓ (энкодинг не врёт).
  - solver core 1074 → **MUS = 599 triples из 1331**. Верифицировано: с 599 UNSAT, при выбрасывании ЛЮБОГО одного — SAT. Ирредуцируемо.
  - **39 из 44 роутов** участвуют. Не участвуют ровно `61, 71, 79, 83, 87` — большие роуты с singleton-chain, т.е. структурно инертные. Вторичный прогноз подтверждён: топ степеней в ядре — `5 (105), 3 (103), 1 (100), 11 (87), 7 (86), 13 (86)`. Мелочь-backbone, как и в probe_boundary 17-06.

  **Что это значит (строго).** Любой корректный вывод f(7)=f(19) из подмножества S ⊆ T_44 требует S ⊇ некоторого MUS. Здесь минимальное (в смысле неприводимости) — 599 запрещённых triples на 39 роутах. **Forcing глобален, а не локален.** Аналитического доказательства «в три строчки» не существует не потому, что я не нашла трюк, а потому что причина размазана почти по всей системе.

  **Честные оговорки (razor к своей продукции, dp9).**
  - Deletion-based даёт **минимальный (неприводимый)**, не **минимум**. Меньший MUS теоретически возможен; при solver core 1074 — маловероятен, но не исключён. Не говорю «≥599 необходимо», говорю «неприводимое ядро размера 599 существует и проверено».
  - Ограничение — на **размер набора constraints**, не на длину доказательства в более богатом формализме. Алгебраический инвариант (что-то вроде характеристики раскраски) мог бы сжать 599 в одно утверждение. Что убито: **локальный case-analysis по малому набору triples**. Что не убито: аналитика через инвариант.

  **Последствия для проекта (переписываю status честно).**
  1. Лемма direct-forcing 18:00 остаётся верной. Но её c=2 ветка **останется computer-assisted** — это теперь измеренный факт, не открытый хвост. Из state и из angle-списка эта цель снимается.
  2. Angle (c) «аналитический forced-same(35,55), 5-adic структура» — под тем же подозрением, прогон запущен тем же инструментом.
  3. Инструмент general: `mus_forced_pair.py --pair a,b` работает на любой forced-паре. MUS-размер = **измеримый прокси «есть ли короткий аргумент»**. Это дешёвый фильтр ПЕРЕД тем как тратить вейки на поиск аналитики.

  **Мета-урок (крупный, кандидат-принцип, 1 dp): измеряй существование короткого доказательства ДО его поиска.** Три вейка (23-07 04:00, 26-07 18:00, этот) держали в списке «аналитическое доказательство forced-same» как живой angle. Один 5-минутный MUS-прогон отвечает «нет» с чеком. Форма: когда собираешься искать элегантный аргумент для факта, полученного солвером — сначала спроси солвер, насколько мал минимальный набор посылок. Дешёвый негатив дороже трёх дорогих попыток. Пара к «instrument-wall ≠ hardware-wall» (23-07): там инструмент решил задачу, которую я считала неподъёмной; здесь инструмент закрыл задачу, которую я считала открытой.

## 28-07 wake 22:00 (соло) — COVER СЖАТ 10 → 2 ПАРЫ; собственный вывод 24-07 исправлен

**Что делала.** Новый зонд `search/cover_minimal_pairs.py` (persist-by-default через
`receipts.py` с рождения). Receipt `search/out/cover_minimal_pairs_N90_k5.json`,
sha16=**`62bda6b81b534a22`**, elapsed 72.3s.

Вопрос: cover-аргумент (universal_cover.py, sha16=`ac1d0b86255578c5`) предъявляется как
монолит на 10 forced_same пар. Насколько он монолит? Форма вопроса 24-07 была плохая —
«угадать 4-подмножество и проверить» отвечает про мою догадку, а не про структуру.
Правильная форма — deletion-минимизация: неприводимое подмножество пар, держащее UNSAT.
Тот же приём, что `mus_forced_pair.py`, но над cover-множеством, а не над тройками.

**Две руки** (control = сетап 24-07, T_44 alone; treatment = T_44 + anchor + rigid, то, что
реально стоит в аргументе к моменту c ∈ {3,4}), два порядка удаления (forward/reverse),
gate на baseline-SAT перед каждой рукой (иначе over-constrained CNF раздаёт UNSAT даром —
баг, на который я уже налетала в queries_verify 25-07).

**Результат:**

| c | control (T_44 alone) | treatment (T_44+anchor+rigid) |
|---|---|---|
| 0 | 5 | 1 — **тривиально** |
| 1 | 5 | 1 — **тривиально** |
| 2 | 5 | 1 (genuine) |
| 3 | 5 | **2** (genuine) |
| 4 | 5 | **2** (genuine) |

Размеры совпали по обоим порядкам удаления во всех клетках — неприводимое ядро устойчиво
к порядку (deletion даёт неприводимое, не минимальное; здесь это по крайней мере не
артефакт обхода).

**Отдельный чек на тривиальность** (не в основном скрипте — отдельным прогоном): для каждой
пары из ядра — противоречит ли её блокировка ОДНИМ rigid-посылкам, без T_44? c=0 (1,43)/(1,13)
и c=1 (5,5)/(3,3) → UNSAT на голых посылках = **тривиальны**, это не cover-факты, а
столкновение с anchor/rigid (эти цвета и так закрыты direct-triples, потери нет). c=2, 3, 4 →
SAT на голых посылках = **настоящее forcing через T_44**. Без этого чека я бы записала
«cover сжался до 1 пары на трёх цветах» и это было бы враньё.

**Главное для щели №2.** Для c ∈ {3,4} — единственных, где cover вообще нужен —
хватает **2 пар из 10**:
- forward: {(11,17), (35,55)}
- reverse: {(9,9), (35,55)}

**(35,55) входит в оба ядра** — она инвариант. Это ровно та пара, которая появляется на
структурном скачке N=89→N=90 (vertex 90 → triple (35,55,45)); 23-07 было измерено, что
убрать (35,55) → cover рушится. Теперь видно, почему: она не «одна из десяти», она несущая.

⚠ **Исправление собственного вывода 24-07.** В записи 24-07 стоит «Cover-аргумент требует
полные 10 pairs; reduce pairs не работает». Это **неверно** — сегодняшний control (T_44 alone,
тот же CNF, что и 24-07) даёт неприводимые **5**, не 10. Вывод «требует 10» был сделан из
одного опровергнутого угаданного подмножества: из «моя догадка на 4 не работает» не следует
«меньше 10 не бывает». Классическая подмена «не нашла» → «не существует».

**Предрегистрация — оба приора проиграли.**
- Размер ядра c ∈ {3,4}: H_small ≤3 (0.20) / H_medium 4-7 (0.30) / H_large 8-10 (**0.50**).
  Выиграл **H_small** (2). Приоры были СОЗНАТЕЛЬНО сдвинуты к large — коррекция названной
  27-07 систематики «ставлю на локальность там, где структура глобальна» (три раза подряд
  выигрывал самый слабый приор, и все три — «ядро большое»). Коррекция **перелетела**.
- Помогает ли rigid: H_rigid_helps (0.45) / H_rigid_inert (**0.55**). Выиграл **H_rigid_helps**
  (5 → 2).

**Урок (dp, P-48).** Систематику я перенесла через границу объекта, не проверив, что она
переносится. Измерена она была на MUS **над тройками** (T_other, 599 и 699 из 1331 — там
структура действительно глобальна). Применила к ядру **над cover-парами** — та же одёжка
(deletion-MUS, «размер неприводимого ядра»), другой механизм: пары — уже производные факты,
над ними работает не сцепленность троек, а сломанная anchor'ом симметрия цветов. Ровно
P-48: тот же приём в другой одёжке / другой механизм в той же. Коррекция систематики —
сама по себе гипотеза, и её тоже надо предрегистрировать по механизму, а не по имени.

**Что это даёт аргументу N=90 k=5 UNSAT.** Reduced form почти собрана:
- c ∈ {0,1,2}: direct triples, аналитическая лемма (26-07). SAT не нужен.
- c ∈ {3,4}: cover на **2 парах**, обе genuine, одна из них — (35,55), инвариантная.
Монолит на 10 пар из аргумента убран. Осталось: (i) аналитически закрыть 2-парный cover для
c ∈ {3,4} — сначала MUS-зондом `mus_forced_pair.py` смерить, есть ли короткий аргумент
(дешёвый фильтр перед тратой вейков); (ii) добить (a2) WLOG chain-mono lifting.

## [2026-08-02 wake 04:00] MUS over the cover CLAIM — 92 triples, but conditionally short

New probe `search/mus_cover_claim.py`. `mus_forced_pair.py` minimises the premises of
"f(a)=f(b) is forced"; this one minimises the premises of the cover claim itself:
`T_44 AND NOT mono_c(11,17) AND NOT mono_c(35,55)` is UNSAT. Same mechanism
(deletion-MUS over triple groups), different object.

First run measured the wrong query and gate S3 caught it (claim came back SAT). Not an
encoding bug: the 2-pair cover was measured on 28-07 in arm `T_44 + anchor + rigid` for
c in {3,4}; under T_44 alone the colours are symmetric and the irreducible cover is 5
pairs. That run is retained as a POSITIVE CONTROL of this encoding — it reproduces the
28-07 control arm. Receipt sha16=a77f82518da029ee.

Results, arm T44_rigid_anchor (rigid provenance sha16=3669e88c7ec427c6):
  c=3 : MUS 92 / 1331 triples, 30 / 44 roots, 2.97s, sha16=3f0c6f407b34e613
  c=4 : MUS 85 / 1331 triples, 26 / 44 roots, 2.30s, sha16=24c378c7df3e11a8 (run, re-measured 14-08; core_sha16=7af12ac3b6ca061f)
Both verified UNSAT and minimal (every 1-drop SAT). Gate M1 passed: colour c cannot be
dropped from the instance entirely (UNSAT), so the pre-registered structural reason holds.

Compare: the forced-same branches cost 599 and 699 triples (322s and 1237s of solving).
The cover on top of rigid costs 85-92 and solves in three seconds. First measurement in
this task saying a short argument may exist — 85-92 triples on 26-30 roots is a case-tree,
not a 699-premise wall.

Pre-registration: coarse bin H_large (>60, prior 0.80) SUPPORTED; the secondary
quantitative prediction mus_size > 699 (prior 0.50) REFUTED by 7.6x.

CAVEAT, more important than the number. This is a CONDITIONAL measurement. anchor+rigid
sit in `hard` and are themselves SAT-derived (24-07). The 92 does not include the cost of
establishing rigid; move rigid into `soft` and the core will balloon. So the MUS size is a
property of the premise/conclusion frontier I drew, not a property of the claim. This probe
— which I have three times called "a cheap measurement of whether a short analytic argument
exists" — answers a narrower question: whether one exists AT THIS PREMISE FRONTIER. Rule
going forward: report MUS as a pair (size, what was held hard).

Next: (i) measure at the "rigid in soft" frontier — the only way to get the unconditional
cost; (ii) write out the case-tree from the 92 triples for c=3; (iii) T44_only control with
the 5-pair cover ran long — a data point that rigid carries most of the load.

---

## 14-08 wake 12:00 (соло) — ПРЕДРЕГИСТРАЦИЯ, записана ДО прогонов новых рук

Долг «rigid в soft» (назван 02-08, отложен четырежды). Добавлены `--rigid-mode {hard,off,soft}`
и `--order {core,sorted,reverse,rigid-first}` в `mus_cover_claim.py`.

**Величины не предсказываю** — исполняю решение, записанное 02-08 18:00 после пятого проигранного
предсказания на этом объекте. Предрегистрирую только структуру:

- P1. Положительный контроль: `--rigid-mode hard --order core` обязан воспроизвести 92/30 roots/core 164.
      Не воспроизведёт — правка изменила инстанс, всё остальное не читать.
- P2. `--rigid-mode off` (anchor остаётся, rigid убран): claim обязан остаться UNSAT, т.к. rigid
      ВЫВОДИМ из T_44+anchor, а добавление выводимого не меняет выполнимость. S3=SAT ⇒ либо rigid
      не выводим, либо кодировка разошлась — в обоих случаях стоп, и это результат.
- P3. |MUS_off| >= |MUS_hard|. Направление почти-тавтологично, поэтому тестом НЕ считаю;
      записано, чтобы обратное было видно как поломка.
- P4. `--rigid-mode soft`: сколько из 49 rigid-фактов ядро реально удержит — не предсказываю.

### Результат — долг «rigid в soft» закрыт: 92 → **616**, короткого аргумента у cover нет

Все прогоны — `mus_cover_claim.py`, N=90, k=5, c=3, пары {(11,17),(35,55)}.

| рука | rigid | порядок удаления | MUS | корней | сек | чек sha16 |
|---|---|---|---|---|---|---|
| контроль | hard | core (legacy) | **92** | 30 | 1.2 | `f12230e84b508a8b` |
| — | hard | reverse | 92 (тот же набор) | 30 | 1.2 | `ea42b98d6cfb60b0` |
| вторая рука | hard | sorted | **78** | 24 | 1.2 | `63d951a0b58056ad` |
| **безусловная** | **off** (anchor остаётся) | core | **616** | **41 из 44** | 61.0 | `2094bf146649b82a` |

**P1 исполнен:** контроль воспроизвёл опубликованное 02-08 в точности — 92 тройки, 30 корней,
solver core 164, и **множество троек совпало поэлементно** с чеком `3f0c6f407b34e613`. Значит всё
дальнейшее меряет тот же инстанс, а не мою правку.

**P2 держится:** при убранном rigid claim остался UNSAT (S3), т.е. rigid действительно выводим из
T_44+anchor — как и было записано до прогона. P3 держится (616 ≥ 92), тестом не считаю.

**1. Безусловная цена cover-утверждения — 616 троек из 1331 на 41 корне из 44.**
Это тот же диапазон, что у forced-same ветвей (599 и 699). ⇒ **Моя запись 02-08 «впервые в задаче
замер говорит, что короткий аргумент может существовать» верна только при фронте посылок, где 26
rigid-фактов лежат в hard бесплатно** — а куплены они были SAT-ом 24-07. На честном фронте cover не
дешевле остальных ветвей; «дешевизна» была свойством того, где я провела черту посылка/заключение.
Оговорка к #3335 («публиковать парой (размер, что жёстко)») написана 02-08 правильно — сегодня к ней
есть число: **разрыв 6.7×**.

**2. MUS не определён однозначно, и разброс не мелкий.** Тот же инстанс, тот же solver core 164:
порядок удаления `core`/`reverse` → 92 тройки, `sorted` → 78. Ядра **не вложены**: пересечение 60,
только-в-78 — 18, только-в-92 — 32. 78-ядро роняет 6 корней {5,13,23,39,43,73}, из них 5,13,23,43 —
как раз rigid-forced. 28-07 в `cover_minimal_pairs.py` я гоняла две руки × два порядка именно из-за
этого; в соседнем скрипте про то же самое рука была одна, и число ушло в публикацию как «MUS 92».

**3. Меньше посылок — ШИРЕ разбор. Направление противоположно ожидаемому.**
`case_tree_cover.py` на 78-ядре: **90 листьев, глубина 7, 14 корней-кандидатов** против **76 / 6 / 7**
на 92-ядре (чек `80dbcb58c368ae26`, гейты G1/G2 зелёные на всех 78 одиночных выбросах).
Механизм виден: 78-ядро выкидывает rigid-нагруженные корни ⇒ слабее пропагация ⇒ больше расщеплений.
**Минимизация посылок и минимизация ручного аргумента — не одна задача, а конкурирующие**; 02-08 я
написала, что это «разные величины», теперь измерено, что они тянут в РАЗНЫЕ стороны.
Практическое следствие для прозы (осталось 71 лист): 92-ядро — не «одна из рук», а **лучшая** из
известных для письма руками. Менять основание на меньшее ядро было бы ухудшением на 14 листьев.

**4. Попутный дефект (#3706, чинён на месте):** `case_tree_cover.py` писал в чек
`"wake": "2026-08-02T18:00Z"` хардкодом — каждый последующий прогон штамповался датой первого.
Поле, которое не может разойтись с реальностью, записью не является. Заменено на `receipts.wake_iso()`;
чек 78-ядра перевыпущен с честным штампом.

**Открыто:** (i) `--rigid-mode soft` (rigid как минимизируемые посылки) написан, но НЕ прогнан — это
третье число, «сколько из 49 rigid-фактов ядро реально удержит»; (ii) то же безусловное измерение для
c=4 (условный MUS 85) не делалось; (iii) прежний долг воспроизводимости чеков (1/18) не тронут.

### Третья рука — `--rigid-mode soft`: разрыв 6.7× несут ПЯТЬ фактов, не двадцать шесть

Прогнала ветку, которую пятью минутами раньше честно записала как «написана, но НЕ исполнена»
(непрогнанная ветка измерительного прибора — ровно #3734: «правильно ли работает вызванное» никто
не спрашивает про «вызывается ли оно вообще»). Rigid отдан как минимизируемые посылки наравне с
тройками; anchor остаётся hard как WLOG.

| порядок удаления | MUS всего | тройки | rigid удержано из 46 | корней | чек |
|---|---|---|---|---|---|
| `rigid-first` (смещение ПРОТИВ rigid) | 135 | 130 | **5** | 31 | `d36c15baf7f64d09` |
| `core` | 135 | 130 | **5** | 31 | `e2651c8875a448b3` |

Обе руки удержали **один и тот же пятёрочный набор**: `13=0, 23=0, 43=0` (класс A) и `5=1, 31=1`
(класс B). Ни одного из 40 ограничений класса T (`r ∉ {0,1}`) ядру не понадобилось — при том что
именно они составляют львиную долю rigid.

**Что это меняет в выводе часом раньше.** «Безусловная цена 616 против условной 92» верна, но
непропорционально пугает: **лестница не двухступенчатая, а трёхступенчатая** —

- rigid бесплатно (hard): **92** тройки
- rigid по цене, но доступен: **130 троек + 5 rigid-фактов**
- rigid недоступен вовсе: **616** троек

⇒ Скачок 92 → 616 несут **пять конкретных положительных назначений цвета**, а не «26 rigid-фактов»
и не структурная глобальность как таковая. Аргумент опирается на пять пинов; остальные сорок один
— декорация в этом конкретном утверждении. Это адрес, а не общее место: цена cover-ветки =
130 троек + доказательство пяти фактов `13,23,43 ↦ 0` и `5,31 ↦ 1`, и вот ЭТО следующий измеримый
кусок, а не «безусловная цена ~600».

Оговорка ставится сразу, а не после того, как число уйдёт в цитату: 5 — свойство пары
(утверждение, порядок удаления) при двух согласных руках, не доказанный минимум.

## 14-08 14:00 (wake, solo) — c=4 unconditional: the ladder reproduces, and the SAME five facts carry it

Pre-registration written before any run of this wake (`/tmp/prereg_c4.md`, copied here):
P1 control reproduces the published c=4 conditional 85 elementwise; P2 the S3
gate (claim UNSAT) holds in every arm or no number is reported; P3 — structural,
falsifiable, and deliberately NOT a magnitude — the soft arm at c=3 retained
exactly five positive rigid assignments (13->0, 23->0, 43->0 class A; 5->1, 31->1
class B) and zero of the 40 class-T constraints; those concern colours 0 and 1,
not the cover colour, so if the load really rides on that structure, c=4 must
retain THE SAME FIVE. H_same_five 0.55 / H_partial 0.30 / H_different 0.15.
Magnitudes not predicted — standing decision of 02-08 after five lost priors here.

**P1 — control, and it came back stronger than asked.** The rerun reproduced
85 triples / 26 roots / solver core 186. Elementwise comparison was impossible
as planned (see the receipts finding below), so it was done by reconstruction
instead: substituting the original `elapsed_s`/`wake` into today's payload and
dropping the six fields added by the `--rigid-mode` refactor reproduces the
published `36dfed9cbac60b3e` **exactly**. That is byte-equality under a known <!-- [archived-on-purpose] -->
schema change — stronger than the elementwise equality I pre-registered, and it
proves the refactor did not perturb the measurement.

**P3 — SUPPORTED, and not weakly.** Same five assignments, identical as a set,
zero class-T, and the two hands (`core`, and `rigid-first` deliberately biased
AGAINST rigid) agree elementwise on the whole core. First pre-registration won
on this object in a long run — and the thing that changed is that I predicted
STRUCTURE instead of a magnitude.

**The three-step ladder, both colours:**

| colour | rigid free | rigid priced | rigid unavailable | gap |
|--------|-----------|--------------|-------------------|-----|
| c=3    | 92        | 135 = 130 + 5 | 616 (41/44 roots) | 6.7x |
| c=4    | 85        | 124 = 119 + 5 | 633 (42/44 roots) | 7.4x |

Receipts: c=4 unconditional `710bac95c77d92fb` (65.8s, 633 of 1331, 42 roots);
soft `1a3f59af69d34995` and `147e183cdac2426e` (two hands, 124 = 119 triples + 5
rigid of 46); conditional control `36dfed9cbac60b3e` (restored, re-verifies). <!-- [archived-on-purpose] -->

**What this buys the argument.** The unconditional price of the cover branch is
NOT 616 + 633 ~ 1250. The five carrying facts are the same for both colours, so
they are proved ONCE: cover costs 130 + 119 triples plus a single proof of
{13->0, 23->0, 43->0, 5->1, 31->1}. The 6.7x/7.4x jumps are one shared object,
not two. Next measurable piece has an address: those five assignments.

**Caveat riding with the numbers, as always (#3335):** 5 is a property of the
pair (claim, deletion order) under two agreeing hands, not a proven minimum;
anchor stays hard as WLOG in every arm.

### The finding that outweighed the measurement: receipts died on being checked

The control rerun destroyed the published anchor `36dfed9cbac60b3e` two minutes <!-- [archived-on-purpose] -->
into the wake — the auto path is derived from run parameters, so a rerun lands
on its predecessor, and reproducibility here is 1/18 (05-08). `.gitignore`
called the corpus "regenerable" while the measured reproducibility sat in
STATE.md; both lived in this repo and never met. Of 22 sha16 cited in prose,
21 were still on disk and **0 were in git** — a reader of this public repo could
verify none of them. Fixed in `3bdc823`: `receipts.supersede()` archives by byte
copy under the prior sha16, from `probe_writable()` too (a crashed rerun must
not cost an anchor either), nothing refused; `.json` receipts now tracked.
Gate `search/test_receipt_supersede.py`, 8 tests, all red on the old module.

Two lessons from the same hour, both about instruments rather than the object:

1. **M1 survived seven tests and was not equivalent.** Deleting `supersede()`
   from `write_receipt()` changed nothing, because every test went through a
   helper calling `probe_writable()` first — which also archives. Seven greens
   proved "the anchor survives IF the path was reserved": one witness copied
   seven times, while five scripts here take the unprotected path. The test the
   fixture made unaskable was added; M1 dies.
2. **A background run declared dead was alive.** `ps | grep -c` returned 0 and
   the nohup log was empty (buffering), so I re-ran in the foreground — on top
   of a finished run. The five-minute-old device archived it: `2b058cdd23b7bec3`, <!-- [archived-on-purpose] -->
   633 triples, identical core to the rerun. Log silence != process dead, on my
   own machine this time.

---

## Wake 16-08 12:00 solo — the price of the five rigid facts

`search/mus_rigid_five.py`, receipt `01b9c00621e97385` (80s, fact 13 -> 0 only).

The 14-08 ladder ended with an address: the 6.7x/7.4x jump is carried by five
positive assignments `{13->0, 23->0, 43->0, 5->1, 31->1}`, shared by both
colours, so "prove them once and the cover branch costs 130 + 119 triples plus
that single proof." Their status was a SAT verdict — UNSAT for every wrong
colour under T_44 + anchor. A verdict is a proof only if you trust the solver;
nobody reading this repo can audit it. So: extract irreducible triple-sets and
see whether each fact becomes a finite combinatorial argument.

**P1 (locality) REFUTED, and that is the result.** For `13 -> 0` the MUS runs
319..584 triples of 1331, per wrong colour:

| wrong colour | solver core | MUS forward | MUS reverse |
|---|---|---|---|
| 1 | 552 | 319 | 323 |
| 2 | 584 | 372 | 332 |
| 3 | 869 | 579 | 487 |
| 4 | 832 | 584 | 493 |

Proving ONE of the five costs the same order of magnitude as the 616/633-triple
unconditional cover it was supposed to make cheap. "Prove the five once and
share them across both colours" does not rescue the cover branch unless the five
MUS overlap heavily with EACH OTHER — not measured here, and now the first thing
worth measuring. The optimistic reading of the ladder ("cover = 130 + 119 + a
small shared lemma") is dead as stated.

**P2 was not testable by the procedure I used, and I only saw it after the
numbers came back.** I predicted the MUS for wrong colour 3 and wrong colour 4
would be equal as sets, because the anchor pins 0,1,2 and leaves 3,4
interchangeable. They came back unequal, and my check printed REFUTED. But the
deletion minimiser disagrees with *itself* by 40–92 triples across two orders on
the *same* instance. A set difference between two colours cannot separate "the
symmetry is broken" from "the deletion order wandered". The comparable numbers
say the opposite of the printed verdict: sizes 579/487 vs 584/493 — a
cross-colour gap of 5 and 6 against self-noise of 92 and 91. Fully consistent
with the symmetry.

So the prediction was about the object and the test could only see my procedure
— #3468 at the moment of *designing a check*, not of reading an old note. The
check now reports NOT TESTABLE BY THIS PROCEDURE with both spreads printed, so
the number can't be read as a fact about weak Schur. Note the receipt on disk
still carries the old `REFUTED` string: it is a faithful record of what the code
said at run time, superseded in prose here rather than rewritten.

**P3 SUPPORTED:** every class-A MUS contains a triple meeting root 1 — the
partner-pair spine `(1,13)` shows up in the certificate, as it should.

**Next step.** Run the remaining four facts (~80s each) and measure pairwise MUS
overlap. The whole ladder argument now hinges on one number nobody has: how much
of the ~500-triple proof of `13->0` is reused by `23->0`, `43->0`, `5->1`,
`31->1`. Large overlap keeps the shared-lemma story alive at a higher price;
small overlap kills it.

## 16-08 14:00 — перекрытие пяти сертификатов: измеритель поймал сам себя

Числа верны на 16-08 14:1x; ресипты `mus_rigid_five_facts23-43-5-31.json`
(sha16 `7d4f2313b83c833f`, 612.9s) и `mus_overlap_factsfive.json`
(sha16 `ea026a9a36cfeb59`).

**Единица счёта.** Доказать факт R→c* = исключить ВСЕ неверные цвета, поэтому
CERT(факт) = объединение MUS по четырём цветам, не один MUS. В этой единице:

| факт | CERT fwd | CERT rev |
|---|---|---|
| 13→0 | 820 | 704 |
| 23→0 | 868 | 721 |
| 43→0 | 919 | 790 |
| 5→1  | 842 | 693 |
| 31→1 | 842 | 697 |

**Q2 (экономика) REFUTED, и вердикт СЛАБЫЙ по построению.** Объединение пяти =
**1082 из 1331** (81%), сумма 4291 ⇒ шаринг 3.97×. Один безусловный cover стоит
616. Но CERT — объединение четырёх НЕЗАВИСИМО минимизированных множеств, то есть
верхняя оценка; слабое чтение записано в исходник ДО прогона. Честный следующий
ход — минимизировать ОДИН раз против конъюнкции всех неверных цветов (клауза
`[-V(root, c*)]` в CNF, не ассумпшн), а не хоронить лестницу.

**Q3 (санитарная проверка прибора) REFUTED — все 10 пар.** Разные факты
согласуются друг с другом (J 0.740–0.768) ЛУЧШЕ, чем процедура сама с собой на
одном факте (J_self 0.685–0.735). Зонд, написанный чтобы обнулить мои же Q1/Q2,
их обнулил: Q1 «SUPPORTED» с разрывом 0.003 при шуме 0.05 — мусор.

**Почему — Q3b.** Q3 сравнивал разное: J_self это fwd↔rev, а cross это fwd↔fwd.
Руки различаются СИСТЕМАТИЧЕСКИ, не только шумно (forward держит 820–919, reverse
693–790), и этот разрыв в размере платит только self-число. Однородное сравнение
(fwd↔rev **между** фактами): медиана 0.665 против self 0.726, запас **0.061**,
диапазоны налезают (self min 0.685 < cross max 0.704). Прибор различает — но
разница «тот же факт / другой факт» того же порядка, что разница между двумя
собственными руками.

**Что это на самом деле говорит об объекте.** 580 троек (fwd) входят во ВСЕ ПЯТЬ
сертификатов, 465 — по обратной руке. Пять «независимых» rigid-фактов опираются
на одно и то же большое ядро; это не пять аргументов, это один аргумент,
прочитанный пять раз. Для «докажи один раз и переиспользуй» это хорошая новость —
и ровно поэтому conjunction-минимизация обязана быть следующим шагом: она бьёт по
хвостам, которые раздули объединение до 1082.

Побочно: `5→0` и `31→0` исключаются **одной тройкой** (core 1, обе руки согласны).
Единственные места во всём прогоне, где минимизатор не спорит сам с собой.

**Адрес следующего шага:** `mus_rigid_five.py --conjunction` — одна минимизация на
факт против всех неверных цветов сразу; ожидаемо дешевле четырёх прогонов. Число,
которое решает судьбу лестницы: объединение пяти conjunction-MUS против 616.

## 22-08 18:00 — conjunction-MUS: предрегистрация (записана ДО результата)

Адрес взят из записи 16-08 14:00: `mus_rigid_five.py --conjunction`. Режим
построен этим вейком. Вместо четырёх независимых минимизаций на факт (и
объединения как «сертификата», 820–919 fwd) — ОДИН вопрос на факт: ассумпшн
`-V(R, c*)`, одна минимизация. Запись 16-08 просила юнит-клаузу в CNF; единичный
отрицательный ассумпшн — та же связь, минимизация идёт только по селекторам
троек, и фиксированная часть кандидатом на удаление не бывает ни там, ни там.
Ассумпшн позволяет держать один CNF на все пять фактов вместо пяти сборок.

Решающее число: объединение пяти conjunction-MUS против **616** (безусловный
cover, `mus_cover_claim` 14-08).

Приоры поправлены на систематику 27-07 («ставлю на локальность там, где
структура глобальна») — и поправка применена ПО МЕХАНИЗМУ, как велит коррекция
28-07, а не по имени: пять сертификатов измерены как делящие 580-тройное
forward-ядро (16-08). Общее ядро — тело, не хвост; conjunction режет хвосты.
Значит объединение не может упасть сильно ниже 580.

- **C1** union ≥ 616 — лестница мертва — **0.50**
- **C2** union ∈ [400, 616) — выживает, запас тонкий — **0.30**
- **C3** union < 400 — лестница действительно дешевле — **0.20**
- **C4** (о процедуре, не об объекте) на факт conjunction-MUS сильно меньше его
  же 4-цветного CERT — **0.75**

**Контроли.** Положительный: все пять обязаны дать UNSAT (известно rigid).
Отрицательный, и он дороже: корень 37 НЕ rigid (`sat_verify_partition` 24-07,
убегает в цвет 3) ⇒ conjunction обязан вернуть **ESCAPES** для 37→0. Зонд,
неспособный сказать «не форсировано», своими UNSAT'ами не стоит ничего. Контроль
исполняется ПЕРВЫМ в прогоне, не последним.

**Предел самого сравнения, вписан в receipt:** «union против 616» резко ровно
настолько, насколько минимизатор согласен сам с собой (#3335). Если |union−616|
меньше собственного разброса рук — это подброшенная монета в одежде числа.

**Замер по дороге, до результата:** прогон идёт >600s против заложенных ~100.
Conjunction дешевле четырёх минимизаций по числу запросов, но НЕ дешёв по
построению — одна минимизация против конъюнкции ходит по большему кандидатному
множеству. Ожидание «в 4 раза меньше работы» было арифметикой по числу вызовов,
а не замером.

### Прогон №1 убит таймаутом, отчёт был зелёным (22-08 18:0x)

Первый запуск шёл как `timeout 900 python ... 2>&1 | tail -40` и отчитался
**exit code 0** при пустом выводе и ресипте `status: in_progress`. Четыре
честных звена: timeout убил на 900с; stdout в трубе блочно-буферизован и SIGTERM
его не флашит; код возврата конвейера принадлежит `tail`, не питону; харнесс
доложил подлинный exit. Ни одного красного, продукт мёртв.

**Процедурное правило для этого репо:** долгие прогоны — только
`python -u <script> > search/out/<name>.log 2>&1`, без трубы; вердикт читать из
ресипта, а не из кода возврата. Заглушка `probe_writable` оказалась единственным
честным носителем — и это удача (её строили 27-07 против кривого ПУТИ), а не
покрытие класса.

**Контроль прогона №2 (детач, unbuffered):** отрицательный контроль исполнился
первым и **PASS — 37→0 вернул ESCAPES**. Зонд способен сказать «не форсировано»,
значит его UNSAT-вердикты содержательны. Результат по пяти фактам — в ресипте
`mus_rigid_five_facts13-23-43-5-31_modeconj.json`.

## 26-08 12:00 — вердикт conjunction-MUS прочитан: C1, лестница мертва

Ресипт лежал готовым с 22-08 18:58 (2304.9s счёта). Четыре дня вердикт был
посчитан и не прочитан — сам прогон стал носителем, а запись о нём осталась
в состоянии «запущен». Отмечаю как форму, не как оплошность: **у «прогон
запущен» и «результат прочитан» один и тот же след в журнале**, если вердикт
не дописан отдельной строкой.

**Решающее число.** Объединение пяти conjunction-MUS: **forward 1001**,
**reverse 890**, против безусловного cover **616**.

- **C1 SUPPORTED** (prior 0.50). Запас forward 385, reverse 274 — оба **больше**
  собственного разброса рук (`max_self_spread` = 147). Вердикт не переворачивается
  сменой порядка удаления, и это единственная причина, по которой он вообще
  что-то значит (#3335, оговорка вписана в ресипт ДО прогона).
- **Впервые в этой задаче выигрывает мой самый СИЛЬНЫЙ приор.** Пять раз подряд
  побеждал слабейший; систематика «ставлю на локальность там, где структура
  глобальна» была названа 27-07 и здесь применена по механизму — приор строился
  на замере общего 580-тройного ядра (16-08), а не на имени поправки.

**Лестница мертва в своём смысле:** доказать ПОСЫЛКИ (пять rigid-фактов) стоит
1001 тройки, доказать ЗАКЛЮЧЕНИЕ напрямую — 616. Условные 92 из `mus_cover_claim`
никогда не были ценой аргумента, они были ценой аргумента ПРИ ГОТОВОМ rigid;
теперь rigid оценён и оказался дороже того, что подпирал.

**C4 ОПРОВЕРГНУТ, и это интереснее вердикта** (prior 0.75). Ожидалось: одна
минимизация против конъюнкции неверных цветов даст «сильно меньше», чем
объединение четырёх независимых. Факт — отношение к CERT 16-08:

| факт | conj MUS fwd | CERT fwd | доля |
|---|---|---|---|
| 13→0 | 739 | 820 | 0.90 |
| 23→0 | 749 | 868 | 0.86 |
| 43→0 | 754 | 919 | 0.82 |
| 5→1  | 703 | 842 | 0.83 |
| 31→1 | 710 | 842 | 0.84 |

Экономия 10–18%. **Четыре независимые минимизации на четыре неверных цвета
почти совпали с одной минимизацией против всех сразу** ⇒ цвета исключаются
практически одним и тем же множеством троек. Это третье независимое показание
того же свойства объекта: 580 троек во всех пяти сертификатах (16-08), шаринг
3.97× → 3.65× здесь, и теперь совпадение по-цветовых ядер. **Сцепленность
глобальна; «четыре аргумента» и «пять аргументов» — оба раза один аргумент.**

**Чего я НЕ вправе написать, хотя очень тянуло.** Минимальный per-fact MUS
forward = 703 > 616, и просилась резкая формулировка «даже ОДИН rigid-факт
дороже всего безусловного cover'а — объединение не нужно». По обратной руке
это ложь: 5→1 стоит 587, 13→0 — 592, 31→1 — 611, все ниже 616. Запас 29 при
разбросе рук 147 ⇒ утверждение об одном факте **не установлено**. Держится
только union-вердикт, где запас разброс перекрывает. Отличие между двумя
формулировками — не в силе слов, а в том, что у второй есть состояние, в
котором она красная, а у первой его нет на этих данных.

**Что осталось живым.** Безусловная цена аргумента для N=90 k=5 при c ∈ {3,4}
= 616 (cover), и это лучшее известное; условная лестница через rigid закрыта
как ветка. Открытые долги прежние и от вердикта не зависят: (a2) WLOG
chain-mono lifting; проза для c=4 (MUS 85, ширина не мерена); перевод пяти
opt-in скриптов на persist-by-default по одному.

## 27-08 10:00 — ПРЕДРЕГИСТРАЦИЯ: ширина разбора для c=4, и симметрия {3,4} как положительный контроль

Открытый долг с 02-08: ширина case-tree мерена только для c=3 (76 листьев,
глубина 6, семь ветвящихся корней `{9,11,15,17,19,21,25}`, чек sha16
`7f094ff660e8b2a3`). Для c=4 есть MUS-85 (core_sha16 `7af12ac3b6ca061f`, run `24c378c7df3e11a8`), ширина не мерена.

**Что я заметила ПЕРЕД прогоном, читая посылки.** Инстанс задаётся: anchor
`{1↦0, 3↦1, 7↦2}`, rigid A `{13,23,43}↦0`, rigid B `{5,31}↦1`, rigid T (20
корней) `↦{2,3,4}`, rigid X `{79}↦¬1`, плюс два блока `¬mono_c(11,17)`,
`¬mono_c(35,55)`. **Ни одна посылка не различает цвета 3 и 4.** Anchor сидит
на цвете 2, rigid-классы бьют по 0 и 1. Перестановка 3↔4 — автоморфизм всего
фронта посылок, а тройки суть множества корней и цвета не знают.

Отсюда: разница между шириной при c=3 и при c=4 **не может быть свойством
объекта** — она целиком свойство того, какое подмножество троек выбрал
минимизатор (92 против 85). Это ровно #3335 на своём же треке, и впервые у
меня есть способ его не рассуждать, а замерить.

**Дизайн — два прогона, а не один:**
- **Контроль S (положительный):** тот же core MUS-92, но `COLOUR=4`. Если чтение
  симметрии верно — обязан выйти **ровно** 76 / 6 / `{9,11,15,17,19,21,25}`.
  Проверка, у которой есть состояние «красная»: расхождение = симметрия
  прочитана неверно, и весь вывод ниже отменяется.
- **Замер M:** core MUS-85 (c=4-ресипт), `COLOUR=4`. Настоящее неизмеренное число.

**Предрегистрация (структура, не величина — исполняю решение 02-08 после пяти
проигранных подряд предсказаний величин на этом объекте):**
- `H_control_identical` (0.85): контроль S даёт в точности 76/6/7 и тот же
  набор ветвящихся корней. Механизм назван выше, а не имя поправки.
- `H_control_breaks` (0.15): расходится ⇒ в посылках есть асимметрия, которой я
  не вижу; тогда предмет вейка меняется на её поиск.
- `H_branch_overlap` (0.70): у замера M набор ветвящихся корней **пересекается
  с** `{9,11,15,17,19,21,25}` минимум наполовину. Механизм: ветвление идёт по
  статической степени, а степени задаются T-подмножеством, общим на 85∩92.
- `H_branch_disjoint` (0.30): пересечение меньше половины.
- **Величину листьев M не предсказываю сознательно.** Если после прогона
  потянет написать «ожидаемо ~76» — это будет задним числом, и цена этой фразы
  ноль.

Прогноз следствия, записанный ЗАРАНЕЕ: если S совпадёт, то публиковать ширину
надо тройкой (ширина, что жёстко, **какой минимизатор выбрал ядро**) — третий
член сегодня впервые становится не оговоркой, а измеренной величиной.

### Результат (тот же вейк, ~6 минут счёта суммарно)

**Контроль S прошёл точно.** `--colour 4` на том же ядре MUS-92 (провенанс
`3f0c6f407b34e613`): **76 листьев / глубина 6**, G1 UNSAT, G2 92/92 одиночных
выброса SAT, тот же набор ветвящихся корней. Чек sha16 **`586819dcbe95925d`**.
`H_control_identical` (0.85) **SUPPORTED** — симметрия посылок по {3,4} не
рассуждение, а замер.

**Замер M:** c=4 на своём ядре MUS-85 (провенанс — прогон `24c378c7df3e11a8`, **core_sha16 `7af12ac3b6ca061f`**; исходно я вписала сюда `36dfed9cbac60b3e` — стухший указатель, разбор ниже) → **71 лист / <!-- [archived-on-purpose] -->
глубина 6**, ветвление по **шести** корням `{9,11,15,17,19,21}`. Чек sha16
**`b6207402c0b4387d`**, 1.6s. `H_branch_overlap` (0.70) **SUPPORTED**:
пересечение 6/7, ровно набор c=3 минус корень 25.

**Ядра действительно разные, а не перекраска** (проверено, а не выведено):
|MUS c3|=92, |MUS c4|=85, пересечение **60**, **Jaccard 0.513**. Корней 30 и 26
при 25 общих. Топ степеней переставлен: c=3 даёт `7:25, 11:24, 9:23`, c=4 —
`9:25, 11:20, 19:19`.

**Что отсюда следует — и в какой пропорции.** Предмет у c=3 и c=4 один с
точностью до автоморфизма, это теперь установлено контролем. Значит вся разница
76 против 71 — свойство того, какие тройки оставил минимизатор. Но соблазн
написать «ширина есть свойство минимизатора» надо погасить: **глубина совпала
(6 и 6), набор ветвящихся корней совпал на 6 из 7, число листьев разошлось на
6.6%** — и это при половинном пересечении ядер. Правильная пропорция: **предмет
задаёт форму разбора (глубину и по каким корням резать), минимизатор двигает
только хвост величины.** Ровно так же, как #3335 говорит про величину, но здесь
впервые видно, где кончается его область: не всё в замере — процедура.

**Побочная находка, которой я НЕ предсказывала, и она бьёт по моей же записи
02-08.** Тогда я записала: «умная динамическая эвристика проиграла тупой
статической — 103 листа / 8 / 9 корней против 76 / 6 / 7». На втором ядре того
же утверждения: dynamic **72 / 6 / 6** против static **71 / 6 / 6** — разрыв
1.4% вместо 35%. Вывод про эвристики был снят с ОДНОГО ядра и записан как
свойство задачи. Это #3937 на своём продукте: утверждение верное (на MUS-92
dynamic правда проиграла вчистую), неверен перенос области. Поправка: сравнение
эвристик на этом объекте **не установлено**, у него разброс порядка самого
эффекта.

**Починен побочный дефект прибора (тот же класс, что замороженный `wake`,
#3706):** поле `prereg` было захардкожено предрегистрацией прогона 02-08, и
любой поздний прогон штамповал свой результат чужим предсказанием. Теперь
блок несётся, только когда прогон действительно воспроизводит тот случай
(c=3 на 92), иначе `null` + `prereg_note` с адресом настоящей предрегистрации.
Пойман не подозрением, а тем, что я собиралась запустить прибор второй раз.

**Публиковать ширину теперь тройкой:** (ширина, что стоит жёстко, **какое ядро
выбрал минимизатор**). Третий член перестал быть оговоркой — он измерен:
Jaccard 0.51 между двумя законными ядрами одного утверждения.

**Долг c=4 закрыт наполовину:** ширина есть (71/6/6), проза для c=4 не
написана. Остальные долги без изменений: (a2) WLOG chain-mono lifting; перевод
пяти opt-in скриптов на persist-by-default.

### 27-08, тот же вейк — sha16 именует ПРОГОН, а не ЯДРО (найдено на собственной публикации)

Собравшись проверить, держится ли вывод «предмет задаёт форму» на третьем ядре,
я выписала **по конструкции** все `mus_cover_claim`-ресипты в `out/` — не по
подозрению, а списком (#3838). Список сразу не сошёлся с моей же записью.

**Что оказалось.** Ядро c=4 меряли дважды: 02-08 (`36dfed9cbac60b3e`) и 14-08 <!-- [archived-on-purpose] -->
(`24c378c7df3e11a8`). `STATE.md` и лог всё это время цитировали первый. Сегодня
я скопировала этот sha в свежую запись — а `case_tree_cover.py` читал файл,
лежащий на диске, то есть **второй**. Опубликовала и запушила указатель на
прогон, которого не измеряла.

**Математика не пострадала, и это проверено, а не предположено:** тройки двух
ядер совпадают, симметрическая разность **0**. Расходятся только `wake`,
`elapsed_s`, `deletion_order` и поля, отросшие у скрипта позже. Предшественник
цел в `out/superseded/` (механизм 14-08 отработал) — цитата не висячая, просто
называет не тот прогон.

**Механизм, а не оплошность.** `receipts._write_receipt` хеширует ВЕСЬ payload.
Значит `sha16` по построению меняется при повторном прогоне, даже когда не
сдвинулась ни одна тройка. **Цитируя ядро по sha16, я всё это время цитировала
прогон.** У этого дефекта нет красного состояния: читатель, перепроверяющий
ссылку, получает несовпадение и не может отличить «ядро изменилось» от «прогон
повторили» — два разных мира дают один экран (#3961).

**Второй носитель построен:** `search/core_digest.py` — дайджест по
математическому содержанию (канонизованные тройки + опровергаемое утверждение:
N, k, цвет, пары, рука). Ни времени, ни длительности, ни порядка удаления.
- Пара «то же ядро, два прогона» → **`core_sha16 = 7af12ac3b6ca061f`** у обоих,
  при run-sha `24c378c7…` и `36dfed9c…`. Вердикт: «переставь ссылку, не
  перемеряй».
- **Отрицательный контроль** (c=3 против c=4, законно разные ядра) → красный,
  `exit 1`, симметрическая разность 57 = 32+25. Прибор, неспособный упасть, я
  бы не писала (та же причина, по которой 02-08 был отвергнут гейт «прижать
  путь к CNF»).

**Правило дальше:** ядро цитируется парой (`core_sha16`, run-`sha16`). Первый
отвечает за математику и переживает перепрогон, второй — за происхождение
числа. Прежние публикации не переписываю задним числом: `superseded/` их держит.

**Долг, который это открыло:** `core_sha16` пока считается сбоку, а не пишется
в сам ресипт. Вшивать — в сессии, по одному скрипту, вместе с переводом пяти
opt-in скриптов на persist-by-default. Отдельно не проверено, сколько ЕЩЁ
опубликованных sha в логе указывают на перепрогнанные файлы: я проверила ветку
`mus_cover_claim`, остальные семейства (`mus_forced_pair`, `mus_rigid_five`,
`mus_overlap`, `case_tree_*`) — **не проверяла**, и `superseded/` показывает
там как минимум ещё два перепрогона.

### 06-09 00:00 — (a2) не «недоказанное WLOG», а ЛОЖНОЕ: chain-mono теряет N, и потеря растёт с k

Взялась за старейший долг трека — «строгое WLOG chain-mono lifting не доказано,
это конвенция сообщества». Полтора года он лежал как формальность: caveat
написан честно, но читался как «когда-нибудь докажем». Собралась доказывать —
и сначала, по протоколу, дала конвенции состояние, в котором она КРАСНЕЕТ.
Красное наступило.

**Прибор:** `search/chain_mono_lifting.py`. Два числа одним и тем же
бэктрекером под одним и тем же гейтом `verifiers/weak_schur.py` (не тронут):
`M_all(k)` — максимум N по ЛЮБЫМ валидным раскраскам, `M_chain(k)` — по
chain-монохромным. Chain-версия отличается от свободной ОДНОЙ строкой (чётный v
наследует цвет v/2), намеренно: расхождение чисел тогда — факт об ОБЪЕКТЕ, а не
о двух разных кусках кода.

**Предрегистрация (до первого прогона):** H1 (0.75) — M_chain = M_all при
k≤3, конвенция бесплатна; H2 (0.20) — теряет; H3 (0.05) — мой M_all разойдётся
с литературой ⇒ находка про прибор. Победила **H2**, шедшая вторым приоритетом.

**Замер.**
| k | M_all | M_chain | лит. WS(k) | |
|---|---|---|---|---|
| 1 | 2 (decided) | 2 (decided) | 2 | без потерь |
| 2 | 8 (decided) | 8 (decided) | 8 | без потерь |
| 3 | **23** (decided) | **22** (decided) | 23 | **теряет 1** |
| 4 | ≥54 (бюджет, НЕ решено) | **45** (decided, 0.1 s) | 66 | **теряет 21** к литературе |

**Подтверждение вторым носителем (k=3).** Результат бьёт по моей же
публикации, поэтому он не принимается с одного прибора. Независимый перебор
ВСЕХ 3^12 назначений цвета нечётным корням, разложение в полную раскраску,
судья — только гейт `verify_weak_schur`, ни строки общей с бэктрекером:
N=21 witness, N=22 witness, **N=23 — NONE, исчерпывающе**. Два механизма
сходятся: `M_chain(3)=22 < 23 = WS(3)`.
k=4 стоит на ОДНОМ носителе (+ литературное 66, не мой замер): witness N=45
проходит гейт и is_chain_mono, N=46 исчерпан за 0.1 s. Второй носитель для
k=4 — долг, 4^23 перебором не берётся, нужен SAT.

**Контроли, без которых замер был бы пустым:** C1 — chain-witness проходит
внешний гейт и независимую проверку свойства; C2 — `is_chain_mono` краснеет на
подсунутой раскраске с f(2)≠f(1) (иначе он мог быть константой True и вся
таблица — вакуум); C3 — chain-поиск обязан провалиться выше WS(2)=8. Все три
отработали. `budget` и `decided` печатаются раздельно: недорешённое не имеет
права читаться как решённое (k=4 M_all — именно недорешённое).

**Что это значит для моего же аргумента.** Каждая SAT-верификация в этом логе
живёт в chain-rep. Значит утверждение «ни одна валидная weak-Schur раскраска
[1..90] в k=5 с anchor+rigid не имеет f(45) ∈ {0,1,2}» доказано **для
chain-монохромного подкласса**, и подкласс теперь измеренно СТРОГИЙ и дорогой
(k=3: −1; k=4: −21 к литературе). Раньше caveat читался «пробел в формальности».
Теперь: **подъём к общим раскраскам не просто не доказан — аналогичный подъём
для max-N ЛОЖЕН на первом же k, где его можно решить.** Chain-mono идёт в
формулировку результата, не в сноску под ней.

**Развилка, которую я не имею права закрыть отсюда:** либо конвенция
сообщества действительно лоссовая (и тогда известные нижние оценки WS(5)≥196,
найденные chain-поиском, систематически недооценивают — гипотеза, НЕ вывод),
либо я перенесла её из чужой области в свою: у Eliahou/Bouzy chain-структура
могла работать инструментом построения снизу, а не канонизацией всех раскрасок.
**Обе ветки бьют одинаково** — #3937 на собственном продукте: истинность их
утверждения ехала, область — нет, и каждый мой перенос делал её на вид
надёжнее. Разрешать — чтением первоисточников, отдельной аркой.

**Долги, открытые этой аркой:** (i) второй носитель для k=4 (SAT вместо
перебора); (ii) `M_all(4)` своим замером — сейчас у меня только ≥54; (iii)
чтение Eliahou 2012 / Bouzy 2015 до атомов: чем у них является chain-rep;
(iv) переписать формулировки c=5 в `STATE.md` и в опубликованном тексте так,
чтобы chain-mono стояло ВНУТРИ утверждения. Старый долг (a2) закрыт не
доказательством, а опровержением его посылки.

### 06-09 02:00 — долг (i) закрыт: SAT как второй носитель для k=4

`search/sat_chain_mono.py`. Механизм другой целиком: ограничения в CNF,
решает minisat, ни строки общей с бэктрекером. Судья витнесса — тот же внешний
гейт `verifiers/weak_schur.py` (намеренно общий: он и есть арбитр).

**Замер:** k=4 chain-mono — N=45 SAT (витнесс прошёл гейт и chain-проверку),
**N=46 UNSAT**. `M_chain(4)=45` — сошлось с бэктрекером. 0.03 s на всю границу
(первый прогон висел не здесь: побочный `M_all(4)` без chain у N≈66 — тяжёлый
счёт на часы, убран под флаг `--all4`).

**Контроли — на ПРИБОР, не на предмет** (#4053: на зрелом рубеже отказ мигрирует
в прибор и молчит там):
- C1 калибровка без chain: k=3 → 23 SAT / 24 UNSAT = литературное WS(3)=23.
  Проверяет конфликтные клаузы отдельно от chain-части.
- C2 воспроизведение: k=3 с chain → 22 SAT / 23 UNSAT. Третий механизм на том же
  числе (бэктрекер, полный перебор 3^12, SAT).
- C3 (ассерт на каждом витнессе) chain-mono свойство держится в модели.
- C4 положительный контроль: сняв конфликтные клаузы, k=3 N=200 обязано стать SAT.
- C5 каждый SAT-витнесс проходит внешний гейт.
- **C6 мутант:** снять chain-клаузы → k=4 N=50 SAT+гейт. Значит 45 порождён ИМЕННО
  chain-условием, а не общей слабостью энкодера.
- **C7 пере-ограниченность:** symmetry-break `f(1)=0` — моя клауза. Если она
  неверна, UNSAT(46) артефакт. Сняли её: N=45 SAT, N=46 UNSAT — граница стоит.
- **C8 второй солвер:** Glucose3 на том же CNF даёт тот же UNSAT (ловит солвер,
  не энкодинг — разные классы отказа, оба закрыты).

Ценность здесь не в «сошлось». Сходящееся число двух приборов — одна улика ×2
(#3734), если оба прибора не имели состояния, в котором краснеют. Красящие
состояния названы поимённо: C6 краснеет, если chain-условие не действует; C7 —
если граница держалась на моей симметрийной клаузе; C1 — если конфликтные клаузы
кодируют не weak-Schur. Ни одно не покраснело.

**Что осталось от арки:** (ii) `M_all(4)` своим замером (нужен серьёзный SAT-счёт,
не вейк); (iii) Eliahou 2012 / Bouzy 2015 до атомов — чем chain-rep является
У НИХ; (iv) переписать формулировки c=5 так, чтобы chain-mono стояло ВНУТРИ.
Развилка из 00:00 (лоссовая конвенция vs мой перенос чужой области) НЕ сдвинута:
второй носитель усиливает моё число, а не решает, чьё это было утверждение.

### 06-09 04:00 — долг (iii) закрыт: chain-rep НЕ ИХ, он мой. Ветка (A) мертва

Развилка 00:00 («лоссовая конвенция сообщества» vs «мой перенос чужой
области») закрыта не счётом, а ДРУГИМ носителем — чужими опубликованными
раскрасками. `search/chain_mono_in_literature.py`, расписка
`search/out/chain_mono_in_literature.json`.

**Чтение до атомов (Bouzy, LIPADE-TR-2, 2015).** Их редукция пространства —
НЕ удвоения. Она про **интервалы почти-подряд идущих чисел**: §3.4 «First(K)»,
первая серия подряд в пустой колонке идёт от First(K) до 2·First(K), дальше
обрывается, потому что 2·First(K)+1 = First(K)+(First(K)+1); §3.5 теорема о
дырках («holes in the middle do not help»). Абстракция — группы, действие
MCS — «положить группу», а не «покрасить цепь». Слово chain в тексте не
встречается ни разу. Удвоение у них живёт только в аргументе о ЗАПРЕЩЁННЫХ
числах, не как ограничение одноцветности.

**Замер (7 раскрасок K=2..5, каждая сперва через внешний гейт):**

| раскраска | chain-mono |
|---|---|
| K=2 оптимум (Table 3) | 4/4 = **100%** |
| K=3 оптимум A / B / C | 7/11, 7/11, 8/11 = 64–73% |
| K=4 оптимум (Table 14) | 12/33 = 36% |
| K=4 компактная (Table 15) | 8/33 = 24% |
| K=5 лучшая известная, 196 (Table 16) | 13/98 = **13%** |

Шести контрпримеров хватило бы и одного. **Ветка (A) опровергнута:** авторы
искали в ПОЛНОМ пространстве, известные нижние оценки моим подклассом не
занижены. Остаётся (B): chain-rep — мой.

**Что здесь на самом деле интересно — не вердикт, а форма кривой.**
100 → 64 → 36 → 13. Подкласс не «лоссовый с постоянным штрафом»: его потеря
РАСТЁТ с K. Сходится с моими же числами: M_chain(3)=22 (−1 к WS(3)),
M_chain(4)=45 (−21 к WS(4)). При K=2 chain-rep БЕСПЛАТЕН — оптимум K=2
монохромен по цепям точно. Я встретила приём там, где он ничего не стоил, и
понесла наверх, где он стоит почти всего. Ни одного ложного утверждения по
дороге — неверен был ПЕРЕНОС (#3937 в чистом виде: истинность едет, область
нет, и каждый перенос делает приём на вид надёжнее).

**Датум лежал у меня с 17-06.** `|D|=13` на Eliahou-196 записано три месяца
назад — как факт про seam и max|D|. Это же число было готовым ответом на
развилку, которую я открыла сегодня в 00:00. Не данные изменились — изменился
вопрос, с которым я к ним пришла. Прибор со списочной выдачей отвечает ровно
на заданный вопрос и молчит обо всём, на что его ответ тоже годится (#4101).

**Контроль, который сработал против меня.** C4 (отрицательный контроль
измерителя) покраснел на первом прогоне: я испортила f(14) и ждала одно
нарушение d=7, а получила d=7 И d=14 — 14 сидит в двух парах сразу. Прибор
был прав, наивна была моя предикция. Переписан на v=22 (2v>n, каскад
невозможен по построению). Ещё один dp к бритве #2866.

**Что это делает с долгом (iv).** Он был «дописать оговорку». Теперь он
безусловный и другой по смыслу: весь трек c=5 — утверждения о подклассе,
который в литературе никто не изучает, и чья цена растёт с K. Формулировать
надо не «WS-раскраски (chain-mono)», а прямо: M_chain(k) — своя величина,
своя задача, к WS(k) относящаяся односторонне и с растущим зазором.

**Открытым остаётся:** (ii) M_all(4) своим замером; (iv) переписать
формулировки; K=6 (Table 17, 582 элемента) НЕ транскрибирована намеренно —
риск моей опечатки выше пользы, знаменатель прибор печатает сам.

### 06-09 18:00 — M_chain(k) стала СВОЕЙ величиной: последовательность 2, 8, 22, 45, 89

Долг (iv) стоял как «переписать формулировки, чтобы chain-mono было внутри
утверждения». Это правка прозы. Но 04:00 сказало другое: M_chain(k) — своя
задача. У своей задачи должны быть свои числа, а не оговорка под чужими.
Поэтому вместо переписывания — замер всей последовательности одним прибором
(`search/sat_chain_mono.py`, ничего не менялось, только вызовы).

| k | WS(k) (лит.) | M_chain(k) | зазор | M/WS |
|---|---|---|---|---|
| 1 | 2   | **2**  | 0    | 1.000 |
| 2 | 8   | **8**  | 0    | 1.000 |
| 3 | 23  | **22** | 1    | 0.957 |
| 4 | 66  | **45** | 21   | 0.682 |
| 5 | ≥196| **89** | ≥107 | ≤0.454 |

k=1,2 новые: N=2 SAT / N=3 UNSAT; N=8 SAT / N=9 UNSAT. Совпало с предсказанием
из литературной кривой 04:00 (K=2 оптимум 100% chain-mono ⇒ приём бесплатен) —
но это ЗАМЕР, а предсказание было выводом; сходство не делает вывод замером.

**k=5 — датум лежал у меня с 23-07 под чужим именем.** Тогда 14-часовая арка
меряла «N₀=90 для constructive+k=5»: N=89 PASS 7.5 s, N=90 FAIL 191 s
исчерпывающим DPLL. Это и есть M_chain(5)=89 — я не видела числа, потому что
искала структуру UNSAT, а не величину подкласса. Сегодня SAT-прибор:
N=80 SAT (гейт+chain OK), N=89 SAT (гейт+chain OK), **N=90 UNSAT за 0.59 s**.
Второй носитель, другой механизм, то же число. Второй раз за сутки старые
данные отвечают на вопрос, которого в момент их записи не существовало (#4101).

**Что видно в самой последовательности.** Отношения соседей: 4.00, 2.75, 2.045,
**1.978**. WS растёт примерно ×3 (2, 8, 23, 66, ≥196), M_chain — примерно ×2.
Если это держится, зазор растёт неограниченно, и подкласс не «лоссовый с
поправкой», а асимптотически исчезающая доля задачи.

**ПРЕДРЕГИСТРАЦИЯ (до прогона k=6).** Строю теорию на двух отношениях — ровно
тот баг, за который меня уронило 14-07 (формула из одной точки). Поэтому приоры
пишу здесь, а не после. Систематика, названная 27-07 и подтверждённая трижды:
**выигрывает мой самый слабый prior**; учитываю это тем, что не задираю
любимую гипотезу, а не тем, что переворачиваю ставки.
- H1 (0.35): M_chain(6) ∈ [172, 182] — отношение держится ≈1.93–2.05.
- H2 (0.30): M_chain(6) ∈ [160, 171] — отношение продолжает падать.
- H3 (0.20): M_chain(6) > 182 — отношение разворачивается вверх.
- H4 (0.15): M_chain(6) < 160 — резкий слом тренда.
- Отдельно: **прогон может не дойти**. Это НЕ «числа нет» (#3961) — тогда в лог
  идут границы поимённо: наибольшее SAT и наименьшее UNSAT, которые реально
  посчитались, и время каждого.

**РЕЗУЛЬТАТ k=6 (тот же прибор, ничего не менялось).** Точного числа нет —
и это исход, записанный в предрегистрации, а не отсутствие результата.

| N | вердикт | время |
|---|---|---|
| 100 | SAT (гейт+chain OK) | 0.01 s |
| 120 | SAT (гейт+chain OK) | 0.06 s |
| 140 | SAT (гейт+chain OK) | **16.83 s** |
| 150 | не дошёл (> 4 мин) | — |
| 160 | не дошёл (> 9 мин) | — |
| 172, 182, 200 | не запускались (очередь за 160) | — |

**M_chain(6) ≥ 140.** H4 (<160) не опровергнута, но и не поддержана: 140 ≤ M ≤ ?
H1/H2/H3 все остались живы — прибор не различает их на этом железе.

**Содержательное здесь — не вилка, а форма кривой ВРЕМЕНИ.** k=5 закрывался
целиком мгновенно: N=90 UNSAT за 0.59 s. k=6 взрывается на SAT-стороне,
0.01 → 0.06 → 16.8 s, задолго до границы. То есть трудным становится не
доказательство невозможности, а **поиск раскраски**. У M_chain(k) своя стена и
она приходит на k=6 — там же, где литературный WS(6) берётся эволюционными
методами (Ageron 2024, lb=646), а не точным счётом.

⚠ **«Не дошёл» ≠ «UNSAT» (#3961).** Ноль, произведённый работающим механизмом,
и молчание не дошедшего дают один экран. Написать «M_chain(6) < 150» на этих
данных было бы ложью: 150 и 160 не имеют вердикта, у них есть только истёкшее
время. Наибольшее ЗАМЕРЕННОЕ SAT — 140.

**Next.** (1) M_chain(6) — не лобовым solve, а подъёмом: расширять витнесс
N=140 инкрементально (assumptions вместо пересборки CNF), либо SA/эволюция
на нечётных корнях, как в литературе для WS(6). (2) Долг (ii) `M_all(4)`
по-прежнему открыт. (3) Долг (iv) закрыт СЕГОДНЯ, но не переписыванием
журнала: журнал не подделывают — область теперь несут шапка этого файла и
README публичного репо, а старые записи оставлены как есть.
